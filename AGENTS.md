# AGENTS.md — 项目工作约定

> STATE.md 的"工作流补充"——聚焦"我们怎么一起干"，不重复"项目是什么"。
> 当两边冲突时，STATE.md §5 决策优先。

---

## 角色边界（确认于 2026-09-14）

- **王星云**：方向 + 拍板（灵魂、决策、最终判断、关键节点确认）
- **载曜**：技术 + 想法 / 建议 / 问题 + 记录

## 节奏

- **v0.5 §0 灵魂**：星云口述，载曜记录；不主动插技术反馈
- **触发开发者模式**：星云说"载曜你怎么看"或 §0 定稿后
- **重大决策**：双方共审；载曜出方案+风险，星云拍板

## 文件管理

- **STATE.md**：每节定稿 / W 阶段 commit 后 / 用户明确要求时更新
- **docs/decisions.md**：每次"拍板"进——日期、拍板人、为什么、备选
- **docs/open-questions.md**：打磨过程未解决的问题，节末扫一眼
- **节后小结**：每节定稿后，3 句话写"这节变了什么、为什么、暴露什么新问题"

## 教训（精简，全文见 zaiyao-memory/MEMORY.md）

- **#35** 情绪驱动"全部重来"先问状态，不列选项
- **#32** reset 前先沉淀
- **#24** 默认不复述上下文
- **#9** scope creep 是 token 浪费最大头
- **#11** 凌晨不开新 milestone
- **#22** 工作量估算 ×1.5-2
- **#21** sed 禁用
- **#37** verifier 不信被验对象
- **#38** 环境基线写进 prompt
- **#39** 诚实报告 > 静默掩盖

## 子进程约定（D-01 / D-04 + D-43）

- **主会话**：决策室——拍板、定规则、回星云消息、**决策记账**
- **subagent**：外派——工程实现、配置、文档草稿、批量产出
- **派**：写代码、跑命令、做调研、**任何需要 push + CI 跑 build 验证的改动**
- **不派**：定方向、问问题、改规则、做判断
- **subagent 连续 2 次假阳性 → 主会话直接接手**（D-04）
- **决策室不修代码逻辑**（D-43）：即使改动 < 5 行 / diff < 20 行，只要需要 push + CI build 验证，**决策室必派 subagent**。例外仅限决策记账类（CHARTER / decisions.md / memory / STATE.md / AGENTS.md 的 append 或 timestamp 更新），不动代码逻辑、不动配置
- **决策室验证环路**（D-43）：事实已闭环后不可反复 exec 验证同一事实（< 3 次重复 verify = 失败模式）

## 代码审查纪律（2026-09-20 + D-43 + D-39 / D-44）

- 每个 milestone 收官后必须独立审查；未通过不得进下一阶段
- 决策室越权修代码 = D-43 违反，触发 §7.0 例外条款收紧
- **审查报告必引用 baseline 路径**（D-39-X / 2026-09-23）：审查报告末行必须显式引用 baseline 文件路径，未引用 = 本轮审查未完成，**不得作为解封依据**
- **CI-1 本体 commit 必保留**（D-44-X / 2026-09-23）：M1-B retro C 路径回滚集合 = {`3c21dd1`, `171aabe`}（2 次越权），**不含 `8d3e3be`**；`fbb7275` 父提交 = `8d3e3be`，回滚落点恰好包含它，不要把它当残留再清
- **归档 commit freeze 必守**（D-39 / 2026-09-23）：归档 commit 一旦 push 到远端禁止再 amend；命名基于 SHA 的归档（<sha7>）会因 amend 失效

- **触发式审查 + 频率底线**：每个 milestone 收官 / 涉及 owner·归属·安全边界改动 / 合并 3+ work item 时必查；每两周至少 1 次（**兜底；触发式优先 — 细则见 D-38**）
- **审查员必读 4 项 + 上下文**：产品定义 / 最近 3 天日志 / STATE / 宪法 + `docs/decisions.md` + `STATE.md` + `git log`（**仅作变更范围索引；commit message 中的自我评价不得作为验证结果 — D-33**）+ 改动历史
- **报告格式**：必须分 P0/P1/P2 + 引用行号 + 注明具体内容（不是泛泛「代码质量不好」）；判级标准见 D-32；证据必须真实执行产出 + CI 落地见 D-36
- **独立原则**：不告诉审查员「自审已通过」——让其在不知道结论的前提下独立判断（**brief 中立性细则见 D-33**）
- **作者签字制**：报告作者必须签字；不打回 = 作者接责；**豁免权加锁见 D-35**
- **未通过禁入下一阶段**：本轮 milestone 未审查通过 → 不进下一阶段（**冻结 + tag 见 D-31；二次审查触发见 D-37**）