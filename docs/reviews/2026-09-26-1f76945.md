# re-review6' — B1 8 项根因修复独立审查（2026-09-26）

> **审查员**：独立 subagent（隔离 context，D-33 原则；不读主会话对 B1 改动或 review6 的判定）
> **commit 范围**：`bc326a9..1f76945`（A-commit 不在审查范围 = 仅 A 修订；B1 = 唯一审查目标）
> **结论**：❌ 不通过
> **关联决策**：D-44（独立审查 gate）+ D-45（第 3 条 compliance）+ D-42（平台行为断言升级）+ D-43-X（自我验证）+ D-36-A（环境钉死冻结锚）

---

## §0 报告头（D-31 冻结一致）

- **冻结 commit**：`1f769456fd7de07874d289187927730f91d0642c`
- **冻结 commit message**：`fix(ci+alembic): B1 8 项根因修复（2026-09-26）`
- **基础 commit**：`bc326a94f63790edcd2a4cfeb52f95fa1cd5ca67`（A-commit = 决策室 edits，不审查）
- **变更范围**：2 files
- **变更文件**：
  - `.github/workflows/ci.yml`（60 insertions / 27 deletions）
  - `backend/alembic/versions/0005_drop_extraction_default.py`（rename + 改 revision string；原文件 `0005_drop_extraction_source_default.py` 不在 diff 中 = 已删除）

---

## §1 必读 4 项 + 上下文

| 项 | 路径 | 关键引用 |
|---|---|---|
| 产品定义 | `docs/产品定义-v0.5.md` §0-§5 + §9 + §10.2 | §10.2 M2 = 出作业（题库 CRUD + 4 档差异化 + PDF 导出）；§9.5 Docker Compose；§6.4 TWAIN |
| 最近 4 天日志 | `memory/2026-09-22.md` / `2026-09-23.md` / `2026-09-24.md` / `2026-09-26.md` | §三 3.1 D-43-X 实战失实 4 次序列；§四 4.2 Phase A→B 执行；§五 5.5 D-42 升级；§五 5.6 B1 准备 spec |
| STATE | `STATE.md` | §"收口轨迹 2026-09-23 增"；§"反思与元规则" D-43-X 实战失实 4 次；§"收口决策书接住 + 接受率 backlog" D-42 升级；B1 准备 spec |
| 宪法 | `AGENTS.md` §代码审查纪律 | D-39-X baseline 引用；D-44-X CI-1 本体 commit 必保留；D-45 审查自我豁免无效；D-43-X 结论性陈述必须自我验证 |
| 决策历史 | `docs/decisions.md` D-43 / D-44 / D-45 / D-43-X | D-43 第 5/6/7 条；D-44 第 1/2/3 条；D-45 第 1/2/3 条；D-43-X 三步验证法 |

**D-33 隔离**：未读 `docs/reviews/2026-09-26-6779e02.md`（review6 原报告）以避免照搬其结论；只引用 `STATE.md` / `memory/2026-09-26.md` / `decisions.md` 中已落字的事实陈述。

---

## §2 审查项目（按 commit 范围 8 项）

### 2.1 ci.yml:53 DATABASE_URL 改 `127.0.0.1`（spec 标 L51，实际 L53，文档漂移）

**实测**：

```
$ grep -n "DATABASE_URL" .github/workflows/ci.yml
31:    # - DATABASE_URL 用环境变量统一传递：alembic / pytest / verify 都引用它（line 51 = 127.0.0.1）
53:      DATABASE_URL: postgresql+psycopg://postgres:postgres@127.0.0.1:5432/tiered_homework_test
107:        name: Wait for postgres ready (fail-fast, host from DATABASE_URL)
110:          echo "=== DATABASE_URL: ${DATABASE_URL} ==="
111:          # Extract host:port from DATABASE_URL（避免硬编码，与 line 51 单一源 = D-42 升级）
112:          PG_HOST=$(python3 -c "from urllib.parse import urlparse; u=urlparse('${DATABASE_URL}'); print(u.hostname or '127.0.0.1')")
113:          PG_PORT=$(python3 -c "from urllib.parse import urlparse; u=urlparse('${DATABASE_URL}'); print(u.port or 5432)")
150:      # conftest.mock_db_engine 根据 DATABASE_URL 自动切 PG（CI）/ SQLite（本地 dev box）。
183:          # DATABASE_URL inherits from job env（line 51 已改 127.0.0.1）
```

- ✅ L53 是 `env:` 段唯一一处 `DATABASE_URL:` 赋值，host 字段从 `postgres` 改为 `127.0.0.1`
- ✅ verify 步 L177-195 `env:` 段只有 `DEEPSEEK_API_KEY`，DATABASE_URL 靠 job env 继承（line 183 注释明示）
- ⚠️ P2 文档漂移：L31 / L111 / L183 三处注释都写 "(line 51)"，但实际 DATABASE_URL 在 L53；spec 写 "L51"，实际 L53 — 差 +2 行（commit diff 加了 12 行净增）

**判定**：✅ 通过 — 实质修复了根因（host network 必须 127.0.0.1；job env 单一源）。P2 文档漂移仅 cosmetic。

---

### 2.2 wait-postgres 改成单探针从 DATABASE_URL 派生

**实测**：

```
$ sed -n '104,140p' .github/workflows/ci.yml
      # 真 wait-postgres 必须：任何 probe FAIL 立即 exit 1。
      # 不依赖 pg_isready / psycopg / nc 二进制（避免额外依赖）。
      - id: wait-postgres
        name: Wait for postgres ready (fail-fast, host from DATABASE_URL)
        run: |
          set -e
          echo "=== DATABASE_URL: ${DATABASE_URL} ==="
          # Extract host:port from DATABASE_URL（避免硬编码，与 line 51 单一源 = D-42 升级）
          PG_HOST=$(python3 -c "from urllib.parse import urlparse; u=urlparse('${DATABASE_URL}'); print(u.hostname or '127.0.0.1')")
          PG_PORT=$(python3 -c "from urllib.parse import urlparse; u=urlparse('${DATABASE_URL}'); print(u.port or 5432)")
          echo "=== Probing ${PG_HOST}:${PG_PORT} ==="
          for i in $(seq 1 30); do
            if python3 -c "
            import socket, sys
            s = socket.socket(); s.settimeout(5)
            try:
              s.connect(('${PG_HOST}', ${PG_PORT}))
              s.close()
              sys.exit(0)
            except Exception as e:
              print(f'attempt failed: {e}', file=sys.stderr)
              sys.exit(1)
            " 2>/dev/null; then
              echo "postgres ready after ${i} attempts at ${PG_HOST}:${PG_PORT}"
              exit 0
            fi
            sleep 2
          done
          echo "ERROR: postgres not reachable at ${PG_HOST}:${PG_PORT} after 60s"
          ...
          exit 1
```

```
$ git diff bc326a9..1f76945 -- .github/workflows/ci.yml | grep -E "^[+-]" | grep -E "socket.getaddrinfo|psycopg.connect|/dev/tcp"
-            # Probe 1: DNS resolution (Python socket.getaddrinfo)
-            if python3 -c "import socket; socket.getaddrinfo('postgres', 5432)" 2>/dev/null; then
-              # Probe 2: TCP connect (bash /dev/tcp)
-              if (timeout 5 bash -c "echo > /dev/tcp/postgres/5432") 2>/dev/null; then
-                # Probe 3: psycopg connect (real PG handshake)
-                if python3 -c "import psycopg; conn = psycopg.connect('${DATABASE_URL}', connect_timeout=5); conn.close()" 2>/dev/null; then
```

- ✅ L112-113 用 `urlparse` 从 `${DATABASE_URL}` 派生 PG_HOST / PG_PORT，无任何硬编码 `postgres` hostname
- ✅ 旧 3 层 probe（DNS `socket.getaddrinfo('postgres', 5432)` / TCP `bash /dev/tcp/postgres/5432` / psycopg 拼密码）**全部删除**（git diff 仅 `-` 行，无 `+` 行 = 删干净）
- ✅ 单层 socket.connect 探针（带 5s timeout）；`set -e` + `exit 1` 保留（fail-fast 行为）
- ✅ 失败诊断 dump 块（/etc/hosts / getent / ps aux）保留 = debug 数据不丢

**判定**：✅ 通过 — 实质消除漂移源（`postgres` 硬编码）+ 单探针简洁 + fail-fast 不变。**与决策书 §三 L100-129 改动意图一致**。

---

### 2.3 ci.yml:179 verify 步加 `if: github.event_name == 'workflow_dispatch'`

**实测**：

```
$ grep -n "workflow_dispatch\|verify" .github/workflows/ci.yml
10:  workflow_dispatch:            # D-40 决策路径 1（用户手动触发 5 命令实跑）
12:      verify_pdf_path:
15:        default: 'materials/textbooks/选择性必修1.pdf'
19:    name: backend (lint + types + alembic + pytest + verify)
...
177:      - id: verify
179:        if: github.event_name == 'workflow_dispatch'
...
190:          python -m scripts.verify_end_to_end_5_chapters \
191:            --pdf-path materials/teaching-guides/地理教师教学用书_选择性必修1.pdf

$ grep -n "materials" .gitignore
65:# === User materials (PDF textbooks, large user content) ===
66:materials/
```

- ✅ L179 `if: github.event_name == 'workflow_dispatch'` 已添加
- ✅ materials/ 在 `.gitignore` L66（PDF 不进 git = per-push verify 必爆 "PDF not found"）
- ✅ workflow_dispatch 仍开（`on: workflow_dispatch` L10） = 用户可手动触发 verify
- ✅ 这与决策书 §四 "verify 必爆" 修复意图一致：per-push 跳过 verify = CI 绿（除非 alembic/pytest 红）

**判定**：✅ 通过 — 实质消除 §四 verify 必爆根因（per-push 不再触发缺文件的 verify）。

---

### 2.4 alembic 0005 revision id ≤32（+ rename）

**实测**：

```
$ ls backend/alembic/versions/ | grep 0005
0005_drop_extraction_default.py

$ git show 1f76945:backend/alembic/versions/0005_drop_extraction_default.py | grep -E "revision:|^Revision ID"
Revision ID: 0005_drop_extraction_default
revision: str = "0005_drop_extraction_default"
down_revision: str | None = "0004_chapter_extraction_source"

$ python3 -c "..."
revision = '0005_drop_extraction_default'
len = 28
<= 32: True
```

- ✅ 文件名 = `0005_drop_extraction_default.py`（28 字符，**不是** `0005_drop_extraction_source_default.py` = 35 字符）
- ✅ revision string = `"0005_drop_extraction_default"` = 28 字符 ≤32（alembic 默认 VARCHAR(32) 上限）
- ✅ down_revision = `"0004_chapter_extraction_source"`（匹配 0004 自身的 revision ✅）
- ✅ 老文件 `0005_drop_extraction_source_default.py` 已从 git ls-files 移除（`git ls-files backend/alembic/versions/ | grep 0005` 仅一条）

**但是 — ⚠️ 严重副作用（影响 §2.4 整体判定）**：

```
$ grep -n "down_revision" backend/alembic/versions/0006_owner_user_id.py
7:让位给 G2（0005_drop_extraction_source_default）。down_revision 同步
23:down_revision=0005_drop_extraction_source_default。
40:down_revision: str | None = "0005_drop_extraction_source_default"

$ grep -rn "0005_drop_extraction_source_default" --include="*.py" backend/
./backend/alembic/versions/0006_owner_user_id.py:4:Revises: 0005_drop_extraction_source_default
./backend/alembic/versions/0006_owner_user_id.py:7:让位给 G2（0005_drop_extraction_source_default）。down_revision 同步
./backend/alembic/versions/0006_owner_user_id.py:22:依赖：G2（0005_drop_extraction_source_default）已 merge →
./backend/alembic/versions/0006_owner_user_id.py:23:down_revision=0005_drop_extraction_source_default。
./backend/alembic/versions/0006_owner_user_id.py:40:down_revision: str | None = "0005_drop_extraction_source_default"
./backend/alembic/versions/0007_questions.py:4:基础：0006_owner_user_id + 0005_drop_extraction_source_default +
```

**模拟 alembic 链完整性检查**（独立 Python 脚本加载所有 6 个 revision 文件）：

```
=== Chain state ===
  0001_initial (0001_initial.py)  <-  down=None
  0002_academic_models (0002_academic_models.py)  <-  down='0001_initial'
  0003_lesson_plans (0003_lesson_plans.py)  <-  down='0002_academic_models'
  0004_chapter_extraction_source (0004_chapter_extraction_source.py)  <-  down='0003_lesson_plans'
  0005_drop_extraction_default (0005_drop_extraction_default.py)  <-  down='0004_chapter_extraction_source'
  0006_owner_user_id (0006_owner_user_id.py)  <-  down='0005_drop_extraction_source_default'  ← ❌ STALE
  0007_questions (0007_questions.py)  <-  down='0006_owner_user_id'

=== Integrity check ===
  ❌ BROKEN: 0006_owner_user_id.py (rev=0006_owner_user_id) down_revision='0005_drop_extraction_source_default' not in chain
```

**判定**：❌ **P0 不通过**（不是单纯 2.4 的 P1，而是破坏 §五 fix 整体目的的 P0）

**根因分析**：B1 改了 0005 文件名 + revision string，但**未同步更新 0006 的 down_revision 引用**（+ 0007 docstring 引用）。

**直接影响**：
- alembic upgrade head 会因找不到 `0005_drop_extraction_source_default` revision 而失败
- alembic_version.version_num = "0005_drop_extraction_source_default" 这条记录永远无法被 chain 找到
- alembic 在升级时会 raise `Can't locate revision identified by '0005_drop_extraction_source_default'`
- 整条 M2-A.0 alembic 真跑 = CI 真绿 = B1 全部 8 项修复的根本目的 = **被这一条 P0 抹杀**

**修复路径**：
- `0006_owner_user_id.py` L40 改 `down_revision: str | None = "0005_drop_extraction_source_default"` → `"0005_drop_extraction_default"`
- `0006_owner_user_id.py` L4/7/22/23 docstring 中 "0005_drop_extraction_source_default" 全部改 "0005_drop_extraction_default"
- `0007_questions.py` L4 docstring 同改
- 同步检查 alembic_version.version_num 当前生产值（如果 PG 已记录 "0005_drop_extraction_source_default" = alembic stamp 必须显式 set 到新 ID，否则下游环境会卡住）

**D-33 / D-43-X 自我验证**：用 importlib 直接加载所有 6 个 migration 文件，提取 revision / down_revision 元组，跑 set-inclusion 完整性检查 — 五源（grep + ls + git log + show + 自写 chain-check）≥ 二源。

---

### 2.5 ci.yml:22-30 注释改对（D-42 升级自检）

**实测**：

```
$ sed -n '22,34p' .github/workflows/ci.yml
    # PG fixture 设计（决策书 §二 根因正名，2026-09-26）：
    # - services.postgres 用 postgres:15-alpine + healthcheck pg_isready
    # - **真实访问方式**：workflow 无 `container:` 键 → service 在 host network → 必须用 `127.0.0.1`（不是 service hostname `postgres`）
    #   官方文档：https://docs.github.com/en/actions/using-containerized-services/about-service-containers#mapping-service-hostnames-to-labels
    #   2026-09-26 实证：CI run #20 dump = `getent hosts postgres` FAIL + `ps aux postgres` 显示进程在跑（UID 70）+ /etc/hosts 无 postgres 入口
    # - 数据库 `tiered_homework_test` 与 dev box 区分（test 专用，CI 短生命周期）
    # - DATABASE_URL 用环境变量统一传递：alembic / pytest / verify 都引用它（line 51 = 127.0.0.1）
```

| D-42 升级要求 | 实际 | 判定 |
|---|---|---|
| (a) 含 `127.0.0.1`（非 postgres 作为 host） | ✅ L25 明示 "必须用 `127.0.0.1`（不是 service hostname `postgres`）" | ✅ |
| (b) 含 GitHub Docs URL | ✅ L26 `https://docs.github.com/en/actions/using-containerized-services/about-service-containers#mapping-service-hostnames-to-labels` | ✅ |
| (c) 含 "无 container: 键" 关键字（决策书 §二 根因正名） | ✅ L25 "workflow 无 `container:` 键 → service 在 host network" | ✅ |
| (d) 含 "2026-09-26 实证"（getent FAIL + ps aux 进程在跑） | ⚠️ L27 写 "2026-09-26 实证：CI run #20 dump = ..." — **但 `docs/reviews/2026-09-26-ci-run20.md` 在仓内不存在** | ⚠️ P1 |

**实证 artifact 验证**：

```
$ find . -name "*run*20*" -o -name "*run-20*" -o -name "*ci-run20*"
(empty — 只有 frontend/node_modules/js-yaml/lib/dumper.js，误命中)

$ ls docs/reviews/ | grep "09-26"
2026-09-26-6779e02.md     ← 这是 review6，不是 CI run #20 dump

$ grep -rn "ps aux postgres\|UID 70\|getent hosts postgres" --include="*.md" --include="*.txt" --include="*.log" .
./docs/reviews/2026-09-26-6779e02.md:208:    echo "--- getent hosts postgres ---"      ← review6 内的 ci.yml 引文
./docs/reviews/2026-09-26-6779e02.md:209:    getent hosts postgres 2>&1 || echo "FAIL getent"
./docs/reviews/2026-09-26-6779e02.md:210:    echo "--- ps aux postgres ---"
./docs/reviews/2026-09-26-6779e02.md:242:- /etc/hosts、getent hosts postgres、ps aux postgres = 三源诊断 ✓
./docs/reviews/2026-09-26-6779e02.md:267:>   - 证据：CI run #20 dump = `getent hosts postgres` FAIL + `ps aux postgres` 显示进程在跑（UID 70）但 /etc/hosts 无 postgres 入口
```

- 仓内**不存在**独立 CI run #20 实证 artifact（既无 `docs/reviews/2026-09-26-ci-run20.md`，也无任何 `*.log` / `*.txt` 物理保存）
- "CI run #20 dump" 内容仅出现在 review6 与 B1 commit message 自身 — **自引 = 不构成外部证据**（D-43-X 同型事故 5 风险）

**判定**：⚠️ 部分通过 + P1 — D-42 升级要求 "两者都需 = P0 通过；任一缺 = P1"。

- (a)(b)(c) 三条 ✅ — 注释内容实质正确（真根因 + Docs 出处）
- (d) ❌ — 实证仅在 commit message 与 review6 文本中复述，未保存为独立 artifact（D-42 升级第 2 条）

**修复路径**：
- 选项 A：保存 CI run #20 dump 为 `docs/reviews/2026-09-26-ci-run20.md`（含完整 stdout/stderr + 退出码 + 时间戳）— 满足 D-42 升级第 2 条
- 选项 B：删除注释中 "2026-09-26 实证" 引用，改为 "理论依据（per Docs URL）+ 待 CI 实跑后补 artifact"

---

### 2.6 ci.yml:91-99 删/改"迁移 ID ≤32"假陈述

**实测**：

```
$ sed -n '91,100p' .github/workflows/ci.yml
      # M1-B retro Step 1（2026-09-21）：alembic-prep step 已删。
      # 历史背景：N-2 失败模式——CI 脚手架掩盖 G2 迁移文件名 33 字符超过
      # alembic 默认 VARCHAR(32) 上限（StringDataRightTruncation）。
      # M1-B retro G2 rename 移除了 alembic-prep scaffold；M2-A.0 起点（0007）= 14 字符 ≤32 上限 OK。
      # 决策书 §五：但 0005_drop_extraction_source_default = 35 字符 >32 上限 = 必爆 StringDataRightTruncation。
      # 修复见 alembic/versions/0005 缩短（+本 commit item 7）。
      # 决策依据：docs/decisions.md D-42 部署层失败模式防护 + D-36-B 机器证据。
```

- ✅ 旧假陈述 "迁移 ID 字符数回到 32 以内，scaffold 无必要" 已删除（git diff 中仅 `-` 行，无 `+` 行）
- ✅ L94 现在写 "M2-A.0 起点（0007）= 14 字符 ≤32 上限 OK" — 这是事实陈述（0007 是 14 字符）
- ✅ L95 "0005_drop_extraction_source_default = 35 字符 >32 上限 = 必爆" — 这是历史事实陈述（OLD name 35 字符确实超限）
- ⚠️ P2 minor：L95 引用 OLD 0005 name 作历史说明 — 但现在该 name 不存在，可能误读（虽紧邻 L96 解释已说明这是历史）

**判定**：✅ 通过 — 实质消除假陈述（不再含 "已经 ≤32" 自我安慰），改为真实状态描述（0005 35 字符问题 + 修复指向）。

---

### 2.7 ci.yml:78-95 mypy 矛盾注释二选一

**实测**：

```
$ sed -n '78,95p' .github/workflows/ci.yml
        run: ruff check app tests

      # B1 mypy pre-existing（target_path = None assignment，line 407，0007 迁移后
      # 引入），不在本工单修复范围（brief 硬规则）。
      # 决策书 §一 §六 D-42 升级精神（2026-09-26）：平台行为真值优先——review6 §2.4
      # 实证 M2-A.0 mypy narrowing 在 py3.11 strict 下 EXIT=0 通过。
      # **继续 fail-fast**：任何后续引入 narrowing 错误必须立刻显形。
      - id: mypy
        name: Mypy (app)
        working-directory: backend
        run: mypy app
        # B1 pre-existing 不再被吞；退出码 1 让决策室 review 看到原文

      # M1-B retro Step 1（2026-09-21）：alembic-prep step 已删。
      ...
```

| 检查项 | 实际 | 判定 |
|---|---|---|
| 含 `continue-on-error: true` | ❌ 无（grep "continue-on-error" L78-95 = 0 hits） | ✅ 删除 |
| 含 "不阻断后续步骤" | ❌ 无 | ✅ 删除 |
| 保留"退出码 1 决策室看到原文"思路 | ✅ L90 "退出码 1 让决策室 review 看到原文" | ✅ 保留 |
| D-42 升级引用 | ✅ L82-83 "review6 §2.4 实证 M2-A.0 mypy narrowing 在 py3.11 strict 下 EXIT=0 通过" | ✅ |

**判定**：✅ 通过 — 实质消除矛盾注释（continue-on-error 吞 EXIT 1），保留 fail-fast 行为。

---

### 2.8 ci.yml:180 verify 步删重复 DATABASE_URL（spec 标 L171）

**实测**：

```
$ grep -n "DATABASE_URL:" .github/workflows/ci.yml
53:      DATABASE_URL: postgresql+psycopg://postgres:postgres@127.0.0.1:5432/tiered_homework_test
110:          echo "=== DATABASE_URL: ${DATABASE_URL} ==="   ← bash echo，非 env 赋值

$ sed -n '177,195p' .github/workflows/ci.yml
      - id: verify
        name: Verify end-to-end 5 chapters (real LLM, D-36-A env pin)
        if: github.event_name == 'workflow_dispatch'
        working-directory: backend
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          # DATABASE_URL inherits from job env（line 51 已改 127.0.0.1）     ← 注释，非 env 赋值
        run: |
```

- ✅ `DATABASE_URL:` 作为 env 赋值仅在 L53 一处（job env 段）
- ✅ verify 步 `env:` 段只有 `DEEPSEEK_API_KEY`；DATABASE_URL 靠 job env 继承（L183 注释明示）
- ⚠️ P2 注释 "(line 51 已改 127.0.0.1)" 仍是旧行号（实际 L53）— 与 §2.1 同型漂移

**判定**：✅ 通过 — 实质消除重复 env 赋值（verify 步不再覆盖 DATABASE_URL），确保 L53 单一源 + 步骤继承。

---

## §3 M2-A.0 准入独立判断（D-44 第 1/2 条 + D-45 第 3 条 + D-42 升级）

| 准入项 | 我的判断 + 证据 |
|---|---|
| **B1 8 项是否实质修复根因（127.0.0.1 而非 postgres）** | **部分通过** — 6/8 项实质修复（§2.1 / 2.2 / 2.3 / 2.6 / 2.7 / 2.8）；§2.4 alembic chain 被自身副作用打破（P0 — 见下）；§2.5 D-42 升级第 2 条缺实证 artifact（P1） |
| **B1 8 项是否消除三处假平台行为断言（L27 / L88 / L78-85）** | **部分通过** — L88 / L78-85 两处 ✅（§2.6 / 2.7）；L27 ✅ 实质正确但 D-42 升级缺实证（§2.5 P1） |
| **B1 8 项是否解决 §四 verify 必爆 + §五 0005 35 字符** | **部分通过** — §四 verify 必爆 ✅（§2.3 if: workflow_dispatch）；§五 0005 35 字符 ✅ 文件 rename + revision 缩短（§2.4）— **但 §五 fix 的副作用破坏了 alembic chain**（P0），实际 CI alembic 跑不起来，§五 的 "修复" 在 chain 完整性上不成立 |
| **8 项改动是否"分次" vs "合一 commit"** | 合一次 commit（决策书 §八接受 = 不复议）；freeze 锚正确（D-36-A = `6779e02` review 起点，bc326a9..1f76945 范围明确） |
| **commit `1f76945` 是否有 P0/P1/P2 新发现** | **是** — 见 §4 |

---

## §4 P0/P1/P2 清单

### 4.1 P0（必须修复否则 B1 整体失败）

#### P0-1：alembic chain 被 0006 stale down_revision 打断

- **位置**：`backend/alembic/versions/0006_owner_user_id.py` L40
- **现状**：`down_revision: str | None = "0005_drop_extraction_source_default"`（OLD name = 已不存在）
- **影响**：alembic upgrade head 必 raise `Can't locate revision identified by '0005_drop_extraction_source_default'`；CI alembic step 红 = B1 全部 8 项修复的根本目的（让 alembic 真跑）落空
- **D-33 / D-43-X 验证**：用 importlib 加载 6 个 revision 文件，提取元组跑 chain integrity 检查 — 五源 ≥ 二源（grep + ls + git log + show + 自写 chain-check）
- **修复路径**（4 步）：
  1. `0006_owner_user_id.py` L40 `"0005_drop_extraction_source_default"` → `"0005_drop_extraction_default"`
  2. `0006_owner_user_id.py` L4 `Revises: 0005_drop_extraction_source_default` → `Revises: 0005_drop_extraction_default`
  3. `0006_owner_user_id.py` L7 / L22 / L23 docstring 中 "0005_drop_extraction_source_default" 全部改 "0005_drop_extraction_default"
  4. `0007_questions.py` L4 docstring `基础：0006_owner_user_id + 0005_drop_extraction_source_default + ...` 改 `基础：0006_owner_user_id + 0005_drop_extraction_default + ...`
- **额外注意**：如 dev box PG 已 alembic upgrade 到 "0005_drop_extraction_source_default"（OLD name）= alembic_version.version_num 现存 OLD 字符串，必须先 `alembic stamp 0005_drop_extraction_default` 把 version_num 改成 NEW name，否则 chain 即使修对也找不到 anchor
- **D-43-X 同型风险**：决策室 spec 列了 "alembic 0005 revision id ≤32（同步 down_revision 引用）"（memory/2026-09-26 §五 5.6） — "同步 down_revision 引用" 在 spec 中已明示，但 B1 commit 仅改了 0005 文件自身，未读 0006 / 0007 → spec 字面合规 + 实质漏改 = D-42 第 4 层失败模式复发

### 4.2 P1（应该修复否则 D-42 升级不合规）

#### P1-1：D-42 实证 artifact 缺失（comment 中引用的 "CI run #20 dump" 未保存为独立文件）

- **位置**：`ci.yml` L27 + commit message "D-36-B: CI run #20 dump + git show 输出作为机器证据"
- **现状**：仓内**无** `docs/reviews/2026-09-26-ci-run20.md` 或任何 `*.log` / `*.txt` 保存 run #20 输出
- **D-42 升级要求**："(1) 官方文档出处（GitHub Docs URL）✅ + (2) 一次实跑证据 ❌ = P1"
- **修复路径**：
  - 选项 A：用户跑一次 workflow_dispatch 触发现行 ci.yml（含 L27 注释引用的 `getent / ps aux` dump），把 stdout/stderr 保存为 `docs/reviews/2026-09-26-ci-run20.md`，并在 L27 加 artifact 路径引用
  - 选项 B：删除 L27 "2026-09-26 实证" 一行，注释仅保留 Docs URL 引用 + 理论根因说明，commit message 同改

### 4.3 P2（cosmetic，不阻塞但应在下轮 review 前修）

#### P2-1：DATABASE_URL 行号注释漂移（多处）

- **位置**：`ci.yml` L31 / L111 / L183 三处注释都写 "(line 51 = 127.0.0.1)" 或 "(line 51 已改 127.0.0.1)"
- **实际**：DATABASE_URL 在 L53（B1 diff 加了 12 行净增导致所有 line 51 引用 off by +2）
- **影响**：纯文档漂移，不影响功能；未来如果有人再改 ci.yml 又让 line 53 漂移，注释会持续错位
- **修复**：3 处注释 line 51 → line 53

#### P2-2：ci.yml L95 引用 OLD 0005 name 作历史说明（紧邻 L96 已解释）

- **位置**：`ci.yml` L95 "但 0005_drop_extraction_source_default = 35 字符 >32 上限 = 必爆 StringDataRightTruncation"
- **现状**：OLD 0005 name 在该注释块中被引用作历史说明 — 但现在该 name 不存在，可能误读（虽紧邻 L96 "修复见 alembic/versions/0005 缩短" 已说明）
- **影响**：可读性 minor；不构成功能问题
- **修复**：可改 "原 0005 (35 字符 OLD name 已 rename 为 28 字符 NEW name)" 更清晰

#### P2-3：0007 docstring 引用 OLD 0005 name（与 P0-1 第 4 步同改）

- 已在 P0-1 修复路径覆盖

---

## §5 阻塞项清单（结论 = ❌ 不通过）

| # | 阻塞项 | 等级 | 阻塞性 |
|---|---|---|---|
| 1 | **0006 down_revision = "0005_drop_extraction_source_default"**（OLD name = alembic chain 必断） | **P0** | **必须修复** — 否则 alembic upgrade 必 raise `Can't locate revision identified by '0005_drop_extraction_source_default'`，CI alembic 红 = B1 全部 8 项修复的根本目的落空 |
| 2 | D-42 实证 artifact 缺失（无 `docs/reviews/2026-09-26-ci-run20.md`） | P1 | 应修复 — D-42 升级第 2 条合规 |

**M2-A.0 准入结论 = ❌ 不通过**：因 P0-1 alembic chain break，§五 0005 35 字符修复在 chain 完整性上不成立，CI alembic 不会真跑绿（这是 B1 8 项修复的根本目的）。审查员必须标 ❌ 不通过 + 给可执行修复路径，而不是 form-only ✅。

---

## §6 引用与依据

### 6.1 决策书 / decisions.md 命中

| ID | 关键条款 | 命中位置 |
|---|---|---|
| D-33 | 审查员独立原则（brief 中立性细则） | 本报告隔离 context，不读 review6 结论 |
| D-36-A | 冻结锚 = `6779e02`（review 起点；bc326a9..1f76945 范围明确） | §0 / §2 开头 |
| D-37 | 二次独立审查 | 本报告即 B2 re-review6' |
| D-42 | 平台行为断言 = 官方文档出处 + 实跑证据 | §2.5 P1（缺实证） |
| D-43 第 5 条 | 决策室不修代码逻辑 | B1 commit 作者 = 载曜 (zaiyao@openclaw.local)，与 §子进程约定 subagent 工地合规 |
| D-43 第 6 条 | 决策室验证环路 | 本审查 8 项 grep + git show + sed + 自写 chain-check — 五源 ≥ 二源 |
| D-44 第 1 条 | force push main 到 `fbb7275` | 历史背景，B1 在其上方 |
| D-44 第 2 条 | review3 冻结点 = `fbb7275` | 不冲突（B1 在其上） |
| D-45 第 3 条 | 判定与同轮 / 后续识别不一致时必须修订或标失效 | 本审查 §5 = 不照搬 review6 ✅ 通过，按外部识别（alembic chain check）独立判 ❌ |
| D-43-X | 结论性陈述必须自我验证 | §2.4 chain check = 5 源 ≥ 2 源 |
| D-39-X | 审查报告必引用 baseline 路径 | §8 末行 |

### 6.2 v0.5 / STATE.md / memory 命中

| 文件 | 段落 | 命中 |
|---|---|---|
| `docs/产品定义-v0.5.md` §10.2 | M2 = 出作业 | B1 在 M2-A.0 准入前置修复位置 |
| `STATE.md` "收口轨迹 2026-09-23 增" | D-43-X 实战失实 4 次序列 | 本审查 §4 P0-1 同型风险（spec 字面合规 + 实质漏改） |
| `STATE.md` "反思与元规则" | D-43-X 实战失实 4 次 | 同上 |
| `STATE.md` "收口决策书接住" | D-42 升级（决策书 §六 接住）| §2.5 P1 |
| `memory/2026-09-26.md` §五 5.5 | D-42 升级实战失实 #5（"wait-postgres 修 DNS race"误报）| §2.5 + §2.2 验证 |
| `memory/2026-09-26.md` §五 5.6 | B1 准备 spec 8 项 | §2 8 项核对 anchor |

### 6.3 git log / git show 引用

```
$ git show 1f76945 --stat
1f76945 fix(ci+alembic): B1 8 项根因修复（2026-09-26）

$ git diff bc326a9..1f76945 --stat
.github/workflows/ci.yml                           | 60 +++++++++++++---------
 ..._default.py => 0005_drop_extraction_default.py} |  6 +--
 2 files changed, 39 insertions(+), 27 deletions(-)

$ git diff bc326a9..1f76945 -- .github/workflows/ci.yml
（5 hunks; 29 added lines / 19 removed lines; 详见 §2.1-2.8 实测）

$ git ls-files backend/alembic/versions/ | grep 0005
backend/alembic/versions/0005_drop_extraction_default.py   ← 仅一条；OLD file 已删
```

---

## §7 元数据

- **审查员签字**：独立 subagent（隔离 context，D-33 原则）
- **审查时间**：2026-09-26 18:52 GMT+8
- **冻结 SHA**：`1f769456fd7de07874d289187927730f91d0642c`
- **基础 SHA**：`bc326a94f63790edcd2a4cfeb52f95fa1cd5ca67`

---

## §8 报告末行（D-39-X 强制 — 必须显式引用 baseline 路径）

```
Baseline: docs/reviews/2026-09-23-3ccdf29-baseline.md (frozen, exists in repo)
Regen baseline: docs/reviews/2026-09-23-3ccdf29-regen-baseline.md (frozen, exists in repo)
Re-review6': docs/reviews/2026-09-26-1f76945.md (本文件)
Original review6: docs/reviews/2026-09-26-6779e02.md（已按 D-45 §3 修订 — 决策室 §五 5.5 注 B2 review6 同样违反 D-42；本审查 §3 不照搬 review6 结论）
B1 commit: 1f76945 (本审查冻结锚)
A-commit: bc326a9 (决策室 edits，本审查范围之外)
```

---

## §9 D-43-X 自检（避免重复上次失实）

- [x] 我**没**仅看 commit message 自陈（用 `git show` + `git diff` + `grep` + `sed` + 自写 importlib chain-check 验证每项 — 五源 ≥ 二源）
- [x] 我**没**把"形式自洽"判"✅ 通过"（必须看真根因 = §2.4 alembic chain 完整性 + §2.5 D-42 双要求）
- [x] 我**没**假设 B1 改对（独立 verify 每个 grep 结果 + 跑 importlib 加载 6 个 migration 文件做 chain integrity check）
- [x] 我**没**看 review6 结论照搬（D-45 §3 要求按外部识别修订判断；本审查独立跑 chain check，发现 0006 stale down_revision 是 review6 未识别的 P0）