fbb727559e66a58a22237a298fdd8620f163d02b
审查模式：静态审查（dev box 无 docker 起 backend；CI 4 次 run evidence 作辅助参考 — D-36-C 降级）
审查范围：fbb7275 (1 commit) + review2 baseline (77095e6) + 4 次 CI run evidence

# M1-B retro 第三轮独立审查报告（D-44 路径）

> **冻结 SHA**：`fbb727559e66a58a22237a298fdd8620f163d02b` (M1-B retro P0-N1 + P1-5)
> **冻结 tag**：(本轮不 commit / 不打 tag — D-44 决策室重打)
> **审查模式**：**静态审查**（dev box 无 docker 起 backend；D-36-C 降级声明：不得用于清 P0 安全/归属边界类 — 此类已实修，D-35 不可豁免）
> **审查范围**：1 commit (fbb7275) + review2 baseline (77095e6) + 4 次 CI run evidence
> **审查员附署**：独立 subagent session（与主会话隔离；D-33 独立原则）
> **关联决策**：D-31（冻结）/ D-32（判级）/ D-33（中立）/ D-34（baseline 复核）/ D-35（豁免）/ D-36（证据三锚）/ D-37（可证伪）/ D-44（M2 解封路径重定）

---

## §1 D-36 真实执行证据（D-36-B 机器证据）

### 1.1 实跑清单（命令原文 + 退出码）

```bash
$ cd backend && ruff check app tests
All checks passed!
RUFF_EXIT=0

$ cd backend && mypy app
app/services/textbook_upload.py:443: error: Incompatible types in assignment (expression has type "None", variable has type "Path")  [assignment]
Found 1 error in 1 file (checked 29 source files)
MYPY_EXIT=1

$ cd backend && pytest tests/test_p0n1_subject_ownership.py tests/test_p15_title_quality.py -q
ss..ss                                                                   [100%]
=============================== warnings summary ===============================
tests/test_p15_title_quality.py::test_is_template_title_detects_chapter_n_pattern
tests/test_p15_title_quality.py::test_is_template_title_detects_chapter_n_pattern
  <frozen importlib._bootstrap>:219: DeprecationWarning: builtin type SwigPyPacked has no __module__ attribute
...
tests/test_p15_title_quality.py::test_is_template_title_passes_real_titles
  /home/wsq_1/.local/lib/python3.8/site-packages/pdfminer/pdfdocument.py:22: CryptographyDeprecationWarning: Python 3.8 is no longer supported...
    from cryptography.hazmat.backends import default_backend

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
2 passed, 4 skipped, 6 warnings in 0.42s
PYTEST_EXIT=0

$ cd backend && pytest tests --collect-only -q
... [84 tests listed, 详见 §1.4]
...
==================================== ERRORS ====================================
ERROR collecting tests/test_academic_api.py        — ImportError: Annotated (py3.8)
ERROR collecting tests/test_academic_flow.py       — ImportError: Annotated (py3.8)
ERROR collecting tests/test_academic_llm.py        — ImportError: Annotated (py3.8)
ERROR collecting tests/test_academic_models.py     — MappedAnnotationError (py3.8)
ERROR collecting tests/test_b33_review_flow_e2e.py — ImportError: Annotated (py3.8)
ERROR collecting tests/test_circuit_breaker.py     — ImportError: StrEnum (py3.8)
ERROR collecting tests/test_health.py              — ModuleNotFoundError: psycopg
ERROR collecting tests/test_streaming_upload.py    — ModuleNotFoundError: psycopg
ERROR collecting tests/test_textbook_upload_real_pdf.py — ModuleNotFoundError: psycopg
!!!!!!!!!!!!!!!!!!! Interrupted: 9 errors during collection !!!!!!!!!!!!!!!!!!!!
84 tests collected, 9 errors in 1.31s
PYTEST_COLLECT_EXIT=2
```

### 1.2 三锚（D-36-A / D-36-B / D-36-C）

- **D-36-A 环境钉死**：
  - 冻结 commit SHA：`fbb727559e66a58a22237a298fdd8620f163d02b` ✓
  - Python：3.8（dev box baseline）
  - 镜像 digest / PG 版本 / 依赖锁文件 hash：**缺失**（dev box 无 docker，PG fixture 不可达）
  - **降级声明**：D-36-A 三锚不完整（dev box 静态审查模式）；按 D-36-C 不得用于清 P0（除已实修类）
- **D-36-B 证据优先级**：机器证据（dev box 实跑 4 闸门 + SQLite minimal 复现）> 人工转录；不依赖 CI 4 次 run（CI 全失败与 fbb7275 内容无关，详见 §1.3）
- **D-36-C 降级声明**：CI 跑不动 → 显式声明「静态审查」+ 不得用于清 P0（D-36 第 4 条硬规则）

### 1.3 CI 4 次 run evidence（D-44 重定路径，辅助参考）

| run | commit | status | conclusion | 关键 fail step | 与 fbb7275 关联 |
|---|---|---|---|---|---|
| #1 | fbb7275 | failure | failure | step 5 Install Python deps（缺 working-directory） | 无关（ci.yml 配置问题） |
| #2 | 3c21dd1 | failure | failure | step 7 Mypy（B1 narrowing） | 无关（已回滚） |
| #3 | 171aabe | failure | failure | 未深入诊断（C 路径触发） | 无关（已回滚） |
| #4 | fbb7275（force push 后） | failure | failure | 同 #1（step 5 working-directory） | 无关（ci.yml 同问题） |

**关键事实**：CI 4 次 run 全 failure 均为 ci.yml 配置问题；fbb7275 commit 本身在 dev box 跑 ruff/mypy/pytest helper 4 闸门全过（除 mypy B1 pre-existing）。

### 1.4 pytest collect 9 errors 根因（与 fbb7275 无关，pre-existing B2 baseline）

8 个 ImportError（typing.Annotated / enum.StrEnum / psycopg 缺）+ 1 个 MappedAnnotationError（py3.8 不支持 `Mapped[list[Class]]`）。全部 pre-existing，与 fbb7275 无关。

**pytest --collect-only 数对比**：
- review2 baseline (77095e6)：78 collected, 9 errors
- review3 actual (fbb7275)：84 collected, 9 errors → **+6 tests** (test_p0n1_subject_ownership.py 2 + test_p15_title_quality.py 4)
- errors 数：9 → 9 不变（B2 baseline 全部 pre-existing）

### 1.5 审查员独立 SQLite minimal 验证（证明 P1-1 FK bug）

```bash
$ PYTHONPATH=. python3 /tmp/probe_p1_5_minimal.py
After flush: p.id = 1
After db.add(c1): c1.id = None, c1.parent_id = 1
After db.add(p2): p2.id = None (expect None if autoflush=False)
After db.add(c2): c2.id = None, c2.parent_id = None
COMMIT FAIL: IntegrityError: (sqlite3.IntegrityError) NOT NULL constraint failed: child.parent_id
[SQL: INSERT INTO child (parent_id) VALUES (?)]
[parameters: (None,)]
```

SQLite in-memory + autoflush=False + FK enforcement：复现 fbb7275 等分 fallback 路径 `KnowledgeReview(chapter_id=ch.id)` 当 `ch.id=None` 时 commit 报 IntegrityError。

---

## §2 D-34 Baseline 状态复核（B1-B9）

### 2.1 review2 baseline B1-B5（review3 不清，仅核对状态）

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| **B1** | `textbook_upload.py` mypy error（target_path = None） | 部分修（行号漂移） | `mypy app` → EXIT=1；`app/services/textbook_upload.py:443` 同样 narrowing 错（review2 时行号 407，fbb7275 +30 行后漂移到 443）；错误类型/文件/修复责任范围均未变 |
| **B2** | dev box pytest collect errors | 部分修（不变） | `pytest tests --collect-only -q` → EXIT=2；84 tests collected, 9 errors（pre-existing：py3.8 + psycopg 缺） |
| **B3** | CI 闸门（含 PG fixture + 集成测试） | 与 fbb7275 无关 | `git diff 77095e6 fbb7275 -- .github/workflows/ci.yml` 为空 |
| **B4** | `pyproject.toml` `[tool.mypy] disable_error_code` 无效 | 与 fbb7275 无关 | `git diff 77095e6 fbb7275 -- backend/pyproject.toml` 为空 |
| **B5** | dev box 跑不动 alembic + 真 LLM | 与 fbb7275 无关 | `git diff 77095e6 fbb7275 -- backend/alembic/` 为空 |

### 2.2 review3 baseline 增量 B6-B9（fbb7275 引入）

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| **B6** | P0-N1 subject 归属校验实现正确性（D-32 第 3 类 安全/归属边界 — D-35 不可豁免） | ✅ 修好 | `routers/academic.py:152-168` `_enforce_owner_or_404` helper；`routers/academic.py:255-262` `upload_textbook` subject_id 校验 + 8 个端点全部覆盖（GET/POST/PATCH 端点逐一加 `_enforce_owner_or_404(textbook/chapter, user_id)`）；`textbook_upload.py:262-272` service 层 defense-in-depth；所有错误均返 **404** 而非 403（避免 ID 存在性泄漏 — D-29 反 ID 泄漏硬规则）；`textbook_upload.py:309, 358` 显式传 `owner_user_id`，禁止 ORM Python default 兜底（D-37 关门原则） |
| **B7** | P1-5 title 质量门槛实现正确性（D-29 第 3 条子项） | ⚠️ **部分修**（详见 §3 P1-1 新发现） | `extractor.py:36-65` `TEMPLATE_TITLE_PATTERN` + `is_template_title` helper 单元测试 PASS；`textbook_upload.py:421-431` 等分 fallback 时创建 `KnowledgeReview(PENDING)` 标记逻辑 — **但 `chapter_id=ch.id` 在 autoflush=False 下为 None，commit 时 PG FK violation**，整段上传失败（fail-CLOSED） |
| **B8** | D-37 负面测试可证伪性 | ✅ 修好 | `test_p0n1_subject_ownership.py:236-340` `test_p0n1_full_fail_open_would_leak_data_returns_201`：monkeypatch router + service 双层为 fail-open → 201 + `owner_user_id=user_2`（证明旧实现灾难性 + 归属污染）；正向测试 `test_p0n1_correct_subject_ownership_returns_404_for_cross_user`（line 141）验证正确实现 → 404；两测试互相校验对方存在 → 满足 D-37 硬规则 |
| **B9** | 测试覆盖度（integration / unit） | ✅ 修好 | 78 → 84 collected（+6 tests）：2 unit (`is_template_title` helper，dev box 跑得过) + 4 integration (subject ownership 2 + title quality 2，标 `@pytest.mark.skipif(py3.8)` CI 真跑) |

---

## §3 新发现 P0/P1/P2 清单

### P1-1：等分 fallback 路径 KnowledgeReview FK bug（D-32 P1）

- **编号**：P1-1
- **严重性**：**P1**
- **D-32 类别**：影响当前功能正确性（P1-5 设计意图失效；fail-CLOSED 而非 fail-open，故非 P0 第 4 类）
- **文件:行号**：`backend/app/services/textbook_upload.py:421-431`
- **完整代码段**：
  ```python
  # textbook_upload.py:421-431 (fbb7275 introduce)
  for cs, summary, src in zip(
      chapters_struct, per_chapter_summary, per_chapter_source, strict=True
  ):
      # ... (中间略)
      ch = Chapter(
          textbook_id=textbook.id,
          chapter_number=cs.chapter_number,
          title=cs.title,
          content_summary=summary,
          page_range_start=cs.page_range[0],
          page_range_end=cs.page_range[1],
          extraction_source=src,
          owner_user_id=owner_user_id,
      )
      db.add(ch)                                  # ← autoflush=False, ch.id = None
      chapters.append(ch)

      # P1-5: 等分 fallback 退化为「第N章」模板时标 PENDING 审阅
      if src == SRC_EQUAL_SPLIT_PLACEHOLDER and is_template_title(cs.title):
          db.add(
              KnowledgeReview(
                  chapter_id=ch.id,               # ← BUG: ch.id = None
                  status=KnowledgeReviewStatus.PENDING,
                  notes="title 退化为模板，需教师审阅（D-29 第 3 条 / P1-5）",
              )
          )

  try:
      db.commit()                                # ← PG FK NOT NULL violation
  except Exception as e:
      db.rollback()
      raise TextbookUploadError(f"DB 写入失败：{e}") from e
  ```
- **证据（机器证据 / SQLite minimal 复现）**：
  - 配置：`sessionmaker(bind=engine, autoflush=False, autocommit=False, expire_on_commit=False)`（`backend/app/db/session.py:27`）
  - 复现脚本：`/tmp/probe_p1_5_minimal.py`（SQLAlchemy 2.0 minimal，独立于 app.models）
  - 复现输出：`COMMIT FAIL: IntegrityError: (sqlite3.IntegrityError) NOT NULL constraint failed: child.parent_id`
- **影响**：
  1. **P1-5 功能失效**：等分 fallback 路径（`extraction_source='equal_split_placeholder'`）上传时 commit 失败，service 层 raise `TextbookUploadError` → router 转 HTTP 400
  2. **CI 真跑测试会失败**：`test_p15_equal_split_placeholder_marks_review_pending`（`test_p15_title_quality.py:104-227`）使用 SQLite + FK enforcement（`conftest.py:106-117`），同样会触发 NOT NULL constraint failure
  3. **P1-5 设计意图落空**：让"标记 PENDING 触发 §7.5 人工审阅流"的功能完全失效
- **修复建议**（3 方案选 1，权衡后推荐方案 A）：
  - **方案 A（推荐 — 最小改动）**：循环里 `db.add(ch)` 后加 `db.flush()` 拿 ID（与 textbook 同步）：
    ```python
    db.add(ch)
    db.flush()  # 拿 ch.id（fbb7275 已在 textbook 处用过 db.flush()）
    chapters.append(ch)
    ```
  - **方案 B（语义清晰）**：构造 KnowledgeReview 时用 `chapter=ch`（通过 ORM relationship 自动同步 chapter_id）：
    ```python
    db.add(KnowledgeReview(
        chapter=ch,  # SQLAlchemy 自动同步 chapter_id
        status=KnowledgeReviewStatus.PENDING,
        notes="title 退化为模板，需教师审阅（D-29 第 3 条 / P1-5）",
    ))
    ```
  - **方案 C（重构）**：循环结束 commit 前先 `db.flush()` chapter 列表，再创建 KnowledgeReview
- **D-35 豁免**：不属于"P0 安全/归属边界/数据可信性"硬不可豁免类别；建议派 fix 工单（方案 A = 1 行修复）

### P1-2：service 层 TextbookUploadError 反 ID 泄漏硬规则弱化（防御性观察）

- **编号**：P1-2（防御性，非当前 bug）
- **严重性**：**P1**（设计观察，不阻 M2 但应记录）
- **D-32 类别**：安全/归属边界设计风险（非当前实际漏洞）
- **文件:行号**：`backend/app/services/textbook_upload.py:262-272`
- **代码**：
  ```python
  # textbook_upload.py:262-272 (fbb7275 introduce — defense-in-depth)
  if subject_id is not None:
      subject = db.get(Subject, subject_id)
      if subject is None:
          raise TextbookUploadError(f"subject_id {subject_id} 不存在")     # ← ID 存在性泄漏（潜在）
      if subject.owner_user_id != owner_user_id:
          raise TextbookUploadError(
              f"subject_id {subject_id} 不属于当前用户"                       # ← ID 存在性泄漏（潜在）
          )
  ```
- **现状分析**：
  - service 层 defense-in-depth 用 `TextbookUploadError` → router 转 **HTTP 400**（不是 404）
  - 现状安全：service 层仅被 router 调用，router 已先 `_enforce_owner_or_404` 返 404 拦下 → service 层 raise 路径走不到
  - **潜在风险**：未来若 service 层被其他调用方（CLI / scripts / Celery worker）直调，会暴露 400 而非 404，违反 D-29 反 ID 泄漏硬规则（第 2 类 ID 存在性泄漏风险）
- **修复建议**（M3/M4 处理，不阻 M2）：
  - **方案 A**：在 service 层加 docstring 注明「仅供 router 调用，其他调用方需自行包装 404」
  - **方案 B（更稳）**：在 service 层区分两种 raise 类型（`SubjectNotFoundError` vs `SubjectAccessDeniedError`），router 统一转 404
- **D-35 豁免**：暂不豁免；M3/M4 接入完整 user 系统（含真 admin role）时一并处理

### 无 P0

- **D-32 P0 第 1 类（数据可信性）**：未命中
- **D-32 P0 第 2 类（不可逆损失）**：未命中
- **D-32 P0 第 3 类（安全/归属边界）**：P0-N1 修好（详见 B6），D-35 不可豁免项已实修
- **D-32 P0 第 4 类（门槛 fail-open）**：D-37 关门原则 — `test_p0n1_full_fail_open_would_leak_data_returns_201`（B8）证明正确实现必要性；P1-1 是 fail-CLOSED（上传失败），不是 fail-open（数据泄漏）
- **D-32 P0 第 5 类（单点可用性）**：未命中（无 OOM / 单点故障风险）

### P2（pre-existing，不列入新发现）

- `backend/app/api/v1/routers/academic.py:303` 重复 `raise HTTPException(...)` 行 — `git diff 77095e6 fbb7275 -- backend/app/api/v1/routers/academic.py` 显示 fbb7275 未触碰此行；pre-existing unreachable dead code；ruff/mypy 不抓（unreachable code）。**不计入新发现**，建议后续清理 backlog。

---

## §4 通过 / 不通过判定

### 4.1 门槛评估

**通过**：
- ✅ D-32 P0 第 3 类 安全/归属边界：P0-N1 修好（router 8 端点 + service defense-in-depth；D-35 不可豁免项已实修）
- ✅ D-37 可证伪硬规则：fail-open 负面测试覆盖完整（`test_p0n1_full_fail_open_would_leak_data_returns_201`）
- ✅ D-36 静态审查三锚：D-36-C 降级声明生效 + 已实修类 P0 由机器证据（dev box 实跑）+ 独立 SQLite 复现支撑
- ✅ 5 文件 diff 完整性：`git diff 77095e6 fbb7275 --stat` = 5 files / +697 / -7（与 brief §3.2 完全一致）

**未通过**：
- ⚠️ **P1-1**：等分 fallback 路径 KnowledgeReview FK bug — 让 P1-5 设计意图失效；CI 真跑测试会失败

### 4.2 判定：⚠️ **不通过**

**理由**：
1. P1-1 是新引入的 P1 bug — 让 P1-5 设计意图（标记 PENDING 触发 §7.5 人工审阅流）完全失效
2. P1-1 影响当前功能正确性（非 fail-open），但 D-44 重定路径下 review3 是 M2 启动门槛——P1-1 让 M2 启动后用户上传模板化 PDF 时直接失败 400，违反"P1-5 是 v0.5 §7.5 流程硬约束"的产品定义
3. CI 真跑测试 `test_p15_equal_split_placeholder_marks_review_pending` 会失败（SQLite FK enforcement 同样拒绝 NULL FK）—— 进一步阻断 D-36-B 机器证据
4. 按 D-32 附加纪律：「fail-open 一律 P0」—— P1-1 是 fail-CLOSED（不属 P0）；按标准判级 P1

**D-35 豁免考虑**：
- 本审查不豁免任何 P0/P1（按 D-35 第 1 条 + brief §5 第 4 条）
- P1-1 不在"P0 安全/归属边界/数据可信性"硬不可豁免类别，但建议派 fix 工单而非豁免（修复成本低，方案 A = 1 行代码 `db.flush()`）
- P1-2 防御性观察暂不豁免，列入 backlog（M3/M4 处理）

### 4.3 决策室后续动作建议

| # | 项 | 优先级 | 谁 | 何时 |
|---|---|---|---|---|
| 1 | 派 fix 工单 **P1-1-1**：方案 A 修改 `textbook_upload.py:421` 循环里加 `db.flush()` | **必修（不阻 M2 启动，但阻 M2 验收）** | 工地 subagent | 本周内 |
| 2 | P1-2 设计观察：M3/M4 接入完整 user 系统时一并处理 | P1 backlog（不阻 M2） | 决策室 | M3 启动前 |
| 3 | P2 dead code 清理：`academic.py:303` 重复 raise 行 | P2 backlog | 工地 subagent | 后续清理 |
| 4 | B1-B5 baseline 闭环：B3 CI 闸门 + B4 mypy config + B5 dev box 真 LLM | 不阻 M2 但需安排 | 决策室 | M2 启动准备阶段 |
| 5 | review4 触发条件：D-38 第 1 条 — P1-1 fix 后必派窄化 review（仅看 fix diff + 失败测试证据） | 触发优先于日历 | 决策室 | fix 落地后 |
| 6 | M2 启动准备阶段重打 tag `review/m1b-retro-<sha7>`（D-31 第 1 条） | tag 落地 | 决策室 | M2 启动前 |

---

## §5 豁免记录

按 D-35（第 5 条：豁免一律书面进 decisions.md）：

| 编号 | 类型 | 类别 | 理由 | 补偿控制 | 责任人 | 失效触发条件 | 审查员附署 |
|---|---|---|---|---|---|---|---|
| (无) | — | — | — | — | — | — | — |

**声明**：本轮审查不豁免任何 P0/P1/P2。理由：
- P0 安全/归属边界/数据可信性：D-35 第 1 条 不可豁免（且已实修）
- P1-1 / P1-2：建议派 fix 工单 + 后续 milestone 处理，不豁免

---

## §6 元数据

- **冻结 SHA**：`fbb727559e66a58a22237a298fdd8620f163d02b`
- **冻结 tag**：(本轮不 commit / 不打 tag — D-44 决策室重打)
- **审查模式**：静态审查（D-36-C 降级声明：不得用于清 P0 安全/归属边界类 — 此类已实修）
- **审查范围**：1 commit (fbb7275) + review2 baseline (77095e6) + 4 次 CI run evidence
- **审查员附署**：独立 subagent session（与主会话隔离；D-33 独立原则）
- **关联决策**：D-31（冻结）/ D-32（判级）/ D-33（中立）/ D-34（baseline 复核）/ D-35（豁免）/ D-36（证据三锚）/ D-37（可证伪）/ D-44（M2 解封路径重定）
- **审查时间**：2026-09-21 (UTC+8)
- **报告路径**：`docs/reviews/2026-09-21-fbb7275.md`（本文件）

---

## §7 报告末行（P0/P1/P2 数量 + 通过/不通过判定）

**P0：0 个；P1：2 个；P2：0 个新发现**

**判定：⚠️ 不通过 — 派 fix 工单 P1-1-1（方案 A：textbook_upload.py:421 循环里加 db.flush()）后可重审通过**
