# M1-B retro 第二轮独立审查档案（D-39 归档）

> **冻结 SHA**: `77095e6054fea0bfd630a5dad132a73f9e197987`  
> **冻结 tag**: `review/m1b-retro-77095e6`  
> **审查模式**: **静态审查**（dev box pytest 全跑跑不动 — 无 psycopg / 无 docker）  
> **审查范围**: 30325f6 / 3499010 / a072127 / 3a71cbb / 259aed3 / 19b1fc2 / 43ff33a / 77095e6（8 commits）  
> **审查员附署**: 独立 subagent session（与主会话隔离；D-33 独立原则）  
> **D-36 限制**: 静态审查 **不得用于清 P0**（D-36 第 4 条）

## 决策室 pre-judgment（基于审查员 §1 baseline 小结）

> "本轮仅靠新增代码层的修复撑住 D-28 数据可信性 + D-29 边界，不可掩盖 CI 缺口"

**整体判定**: ❌ **不通过** — M2 题库 CRUD 启动按 AGENTS.md § 代码审查纪律禁入

**不通过的根本原因**: Baseline B3（CI 闸门含 PG fixture）+ B4（mypy config）+ B5（dev box 真 LLM + alembic）三处仍开，构成 CI 缺口 + 不可执行证据链断裂。按 D-32 第 5 类 P0「单点可用性」+ 第 4 类「门槛 fail-open」（无法在 CI 复跑 = 门槛 proxy 失效）。

---

## §1 D-34 Baseline 状态复核（不计分但计入通过）

| # | 项 | 判定 | 证据 |
|---|---|---|---|
| **B1** | `textbook_upload.py` mypy error（target_path = None） | 部分修（行号漂移） | 43ff33a 加 `owner_user_id` 参数 + 9 行 docstring 后行号 398 → 407；错误类型/文件/修复责任范围均未变 |
| **B2** | dev box pytest collect errors | 部分修（-2 errors, +31 tests） | 工单 B 前 11 errors / 47 collected；工单 B 后 9 errors / 78 collected。剩余 9 errors 全部 pre-existing（psycopg / Annotated / StrEnum / MappedAnnotationError） |
| **B3** | CI 闸门（含 PG fixture + 集成测试） | ❌ **仍开** | `.github/workflows/ci.yml` 当前只跑 ruff + mypy + pytest collect（无 PG service） |
| **B4** | `pyproject.toml` `[tool.mypy] disable_error_code = ["untyped-decorator"]` 无效 | ❌ **仍开** | mypy 输出仍含此 warning |
| **B5** | dev box 跑不动 alembic + 真 LLM | ❌ **仍开** | 无 docker / 无 psycopg |

## §2 D-36 真实执行证据（原始输出）

### 2.1 实跑清单（命令原文 + 退出码）

```bash
$ cd backend && ruff check app tests
All checks passed!
EXIT=0

$ cd backend && mypy app
app/services/textbook_upload.py:407: error: Incompatible types in assignment (expression has type "None", variable has type "Path")  [assignment]
Found 1 error in 1 file (checked 29 source files)
pyproject.toml: [mypy]: disable_error_code: Invalid error code(s): untyped-decorator
EXIT=1

$ cd backend && pytest tests --collect-only -q
PYTEST_EXIT=2
78 tests collected, 9 errors in 1.28s
```

### 2.2 pytest collect 9 errors 根因

```python
# 共用根因链
app/api/v1/routers/academic.py:59: from app.db.session import get_db
app/db/session.py:26:             engine = _make_engine()
sqlalchemy/dialects/postgresql/psycopg.py:495: import psycopg
E   ModuleNotFoundError: No module named 'psycopg'
```

剩余 8 errors 同型（Annotated / StrEnum / MappedAnnotationError）—— 全部 py3.8 限制或 dev box 缺依赖。

### 2.3 未跑项（D-36 第 3 节强制）

| 未跑项 | 原因 | 谁跑 | 何时跑 |
|---|---|---|---|
| `pytest tests -q`（全跑） | dev box 无 psycopg / 无 docker | CI（PG service fixture） | **B3 闭环时必修 — M2 启动硬阻塞** |
| `alembic upgrade head` | dev box 无 psycopg | docker compose（PG 容器） | **B3 闭环时必修** |
| `verify_end_to_end_5_chapters.py` 实跑 | dev box 无 docker 起 backend + 无真 DEEPSEEK_API_KEY | docker exec backend（含真 .env） | **B3 闭环时必修** |
| `mypy app` 含 psycopg 链路的 module | dev box 无 psycopg（engine import 即炸） | CI（PG container 内） | B3 闭环时 |
| `curl POST /textbooks/upload` 真 PDF | 同上 | docker compose 起 backend | B3 闭环时 |

---

## §3 解封条件（M2 启动前必修 3 件）

按优先级：

### P0 解封 1: 修 B4（mypy config warning，1 行修复）
- 文件：`backend/pyproject.toml`
- 改动：`disable_error_code = ["untyped-decorator"]` → 删该项或改正确码
- 提交：决策室直接做（不属工地）
- 期望：mypy 输出不再含此 warning

### P0 解封 2: 修 B3（CI 闸门含 PG fixture + 真 pytest）
- 文件：`.github/workflows/ci.yml`
- 改动：加 `services: postgres:15` + `alembic upgrade head` step + pytest 全跑
- 提交：派工地子会话（CI 配置 + pg fixture 编排）
- 期望：CI green 时能拿到真 pytest 全跑输出 + mypy 在 PG container 内干净

### P1 解封 3: 修 B5（dev box 真 LLM + alembic）
- 文件：本地 docker-compose 启动 backend（已存在）；缺真 DEEPSEEK_API_KEY
- 改动：用户注入真 key 到 `~/.openclaw/secrets/`（决策室不直接处理密钥）
- 提交：用户授权 + 决策室执行
- 期望：`docker exec thp-backend python -m scripts.verify_end_to_end_5_chapters` 能在 dev box 实跑

### 解封后再审（Decision book §4 二次审查触发）
- B3/B4/B5 闭环后 → **派第二次独立审查**（同 D-31 / D-32 / D-33 / D-36 模板；这次报告模式应能升级到「实跑」）
- 冻结 SHA：新的 merge commit

---

## §4 元数据

- 归档时间：2026-09-20 21:14
- 归档决策：决策室按 D-39 落地（本轮不 commit，归档文件进 git 但不强制 tag；tag 在 M2 启动准备阶段重打）
- 报告截断说明：子会话 prompt-data 输出末尾被截（§3 新发现清单 + §4 通过/不通过判定缺失）；决策室基于 §1 baseline 小结已做出 pre-judgment。如需完整 §3 + §4，可 follow-up review subagent 补完（kept session 仍可接收消息）
- baseline 单独冻结路径：`docs/reviews/2026-09-20-77095e6-baseline.md`（如需可由后续工地生成）
