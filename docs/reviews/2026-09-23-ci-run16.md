# CI run #16 实证记录（review/m2a0-3ccdf29 tag 触发）

> **最后更新**：2026-09-24 10:50（**archive 失实修订**——D-43-X 实战失实：原 §3 表"alembic/pytest/verify ✅"全错，实际为 ⊘ skipped；决策书硬-2/3 反证全失效）
> **冻结 SHA**：`3ccdf29` (M2-A.0 本体)
> **触发**：push to tag `review/m2a0-3ccdf29`
> **结论**：整体 ❌ failure（backend job exit 1 — Ruff check 红）+ 9 个 step ⊘ skipped（ruff 后所有 step）
> **关联决策**：D-36-A/B/C + D-45 + D-39-X（末行自引用）+ D-43-X（实战失实记录）
> **维护者**：作者冻结（不得 amend；D-39 第 5 条硬规则）

## §1 Brief（冻结点 + 范围 + 评估类别）

- **冻结 commit**：`3ccdf293b8b27b74454f83d68110dd97ef645784`
- **触发方式**：push to `review/m2a0-3ccdf29` (lightweight tag)
- **评估类别**：M2-A.0 准入前置 — 独立审查 (review5) 静态降级，CI 真跑作为机器证据

## §2 D-36-A 环境钉死

| 锚 | 值 | 来源 |
|---|---|---|
| commit SHA | `3ccdf29` | tag |
| Python | `3.11` (CI runner) | ci.yml L60-69 |
| 镜像 digest | 缺失 ⚠️ | runner 默认 image，未钉死 |
| 依赖版本 | 缺失 ⚠️ | `requirements.txt` 全部 `>=`（决策书 §四新-2 工程债）|
| PG 版本 | `15-alpine` | ci.yml L28-39 |

**D-36-A 完整度**：3/5 锚钉死（SHA + Python + PG），2/5 缺失（镜像 digest + 依赖版本）= 决策书 §六 "D-36-A 只钉了 SHA / Python / PG，没钉依赖版本"

## §3 十五命令真实状态（修订后 — D-43-X 实战失实修正）

### Backend job

| Step | 图标 | 真实状态 | 备注 |
|---|---|---|---|
| Set up job | ✓ | passed (2s) | |
| Initialize containers | ✓ | passed (21s) | services.postgres 启动 |
| Run actions/checkout@v4 | ✓ | passed (1s) | |
| Setup Python 3.11 | ✓ | passed (2s) | |
| Install Python deps | ✓ | passed (16s) | |
| **Ruff check (app + tests)** | **❌ FAIL** | **failed (0s)** | 6 lint errors in `backend/tests/test_review4_p11_fix_repro.py`（4 auto-fixable + 2 F811 重定义）|
| **Mypy (app)** | **⊘ skipped** | **0s** | ruff 失败 → cancelled |
| **Alembic upgrade (PG)** | **⊘ skipped** | **0s** | ruff 失败 → cancelled |
| **Pytest (full run on PG)** | **⊘ skipped** | **0s** | ruff 失败 → cancelled |
| **Start backend (uvicorn)** | **⊘ skipped** | **0s** | ruff 失败 → cancelled |
| **Verify end-to-end 5 chapters** | **⊘ skipped** | **0s** | ruff 失败 → cancelled |
| Post Setup Python 3.11 | ⊘ skipped | 0s | cancelled |
| Post Run actions/checkout@v4 | ⊘ skipped | 0s | cancelled |
| Stop containers | ⊘ skipped | 0s | cancelled |
| Complete job | ⊘ skipped | 0s | cancelled |

**Backend job 整体**：❌ failure（exit code 1 — Ruff 红拖垮 + 后续 steps 全部 cancelled）

### Frontend job

| Step | 图标 | 真实状态 |
|---|---|---|
| Setup Node 20 | ✓ | passed |
| Install dependencies | ✓ | passed |
| ESLint | ✓ | passed |
| TypeScript check | ✓ | passed |
| Build | ✓ | passed |

**Frontend job 整体**：✅ success

### 总结

- **Backend**：5 ✓ passed + 1 ❌ failed (ruff) + 9 ⊘ skipped = 1 红 5 跳 9 skip
- **Frontend**：5 ✓ passed + 0 ❌ + 0 ⊘ = 5 绿 0 红
- **总体**：❌ failure

### D-43-X 实战失实记录（archive §3 自检）

**原版 archive §3 表**（2026-09-24 10:32 commit `5ac7a62` 推送）声明：

> Mypy (app) ✅ | Alembic upgrade (PG) ✅ | Pytest (full run on PG) ✅ | backend-up ✅ | Verify end-to-end 5 chapters ✅

**修订**：以上 5 个 step 全部 ⊘ skipped（ruff 失败后 cancelled）。原 §3 表 5 项 ✅ 应改 5 项 ⊘。

**失实根源**（D-43-X §1 反例）：
- 用户口述："其他为灰色对号"
- 我解读为"✅ 通过"，未做独立路径复跑（用户贴截图 / jq GitHub API / 看图标细节）
- 应做：要求用户贴每个 step 的真实截图 + 我独立 jq `conclusion` 区分 ✅ vs ⊘
- 教训 = D-43-X §2："异常/缺失/通过/跳过" 四态必须独立路径复跑一次确认

## §4 decision book 硬-1/2/3 实测结果（修订后）

| 决策书说法 | CI 实证 | 判定 |
|---|---|---|
| **硬-1**: review5 自我豁免 D-32 P0 第 3 类安全/归属边界 | **pytest ⊘ skipped** — 无 CI 证据 | **未验 / 无法证伪**：CI 未真跑 pytest，review5 的"代码视觉 + 存在性"论证路径**未受 CI 反证**——硬-1 仍成立 |
| **硬-2**: mypy 0 errors 是 dev box cache 漂移假绿 | **mypy ⊘ skipped** — 无 CI 证据 | **未验 / 无法证伪**：CI 未真跑 mypy，dev box mypy EXIT=1 归因需另行调查（决策书 §三修-1 提出 = mypy 版本不锁 vs py 版本差异）|
| **硬-3**: M2-A.0 从未执行 | **alembic + pytest + verify ⊘ skipped** — 真未执行 | **完全成立**：CI run #16 是 ruff failure → 后续全部 cancelled，**M2-A.0 在 run #16 未真执行**（决策书硬-3 成立）|

## §4.5 Ruff 失败详情

```
backend/tests/test_review4_p11_fix_repro.py:
  F401 [*] `tests.conftest.mock_db_engine` imported but unused (line 8:5)
  F401 [*] `tests.conftest.FIXTURE_NAME` imported but unused (line 9:5)
  F811 redefinition of unused `mock_db_engine` (line 12:8)
  F811 redefinition of unused `FIXTURE_NAME` (line 13:8)
  F541 f-string without any placeholders (line 41)
  F541 f-string without any placeholders (line 66)
6 errors, 4 fixable with --fix
```

**关键**：
- 该文件由 7ff30aa 引入（73 lines），216a0c1 删除（73 deletions）
- 3ccdf29（tag 冻结点）= 文件**存在**（在 216a0c1 删除之前）
- HEAD（5ac7a62）= 文件**不存在**（216a0c1 删除之后）
- HEAD CI 重跑 = ruff 应 ✅（repro file 已被删）

## §5 M2-A.1 准入矩阵（修订后）

| 准入项 | 真实判定 | 依据 |
|---|---|---|
| 冻结基线三方一致 | ✅ | HEAD = `5ac7a62` 三方一致（local + origin + github 都已 push） |
| 工作树干净 | ✅ | S1-⑥ commit (`5ac7a62`) 已绑 D-45 三文件 + memory/2026-09-24.md + ci-run16 archive |
| 迁移链可 apply | **❌ 未验** | run #16 alembic ⏮ skipped；HEAD 触发 CI 应验证但当前未跑 |
| 归属边界 P0 有机器证据 | **❌ 未验** | run #16 pytest ⏮ skipped；HEAD 触发 CI 应验证但当前未跑 |
| mypy / 类型门禁 | **❌ 未验** | run #16 mypy ⏮ skipped；HEAD 触发 CI 应验证但当前未跑 |
| 端到端 LLM 验证 | **❌ 未验** | run #16 verify ⏮ skipped；HEAD 触发 CI 应验证但当前未跑 |
| 独立审查结论有效 | ⚠️ | review5 = 静态降级 + 自我豁免，未按 D-45 第 3 条修订；M2-A.1 准入后需补修订 |
| 机器证据已落盘 | ✅ | 本文件即修订后落盘 |

**修订结论**：4 项 ❌ 未验 + 1 项 ⚠️ = M2-A.1 **不能开**（实质条件未满足）。需在 HEAD 触发 CI 全绿 + review5 §6.1 修订后方可。

## §6 D-39-X 自带出口检查

本文件 = `docs/reviews/2026-09-23-ci-run16.md`（机器证据落盘，**内容已修订为真实状态**）。

---

**关联 baseline 文件**：`docs/reviews/2026-09-23-3ccdf29-baseline.md`（review5 frozen baseline）

**D-39-X 末行自引用**：

```
Baseline: docs/reviews/2026-09-23-3ccdf29-baseline.md (frozen) + docs/reviews/2026-09-23-ci-run16.md (machine evidence, revised)
```

## §7 来源（修订后）

收口决策书 2026-09-24 §三 / §四 / §六 + **GitHub Actions 截图（用户提供 run #16 step 图标 = 真实 ⊘ skipped 状态）** + git 实测（HEAD = 5ac7a62 三方一致）+ 9/22/23 memory + decisions.md D-43/D-44/D-45/D-43-X。

---

**修订者注**：本文件 2026-09-24 10:32 commit `5ac7a62` 版本（`chore(meta): S1-⑥ 决策室补登 2026-09-24 收口决策书接住`）的内容**严重失实**——把 9 个 ⊘ skipped step 误写为 ✅ passed。10:50 修订版修正为真实状态。