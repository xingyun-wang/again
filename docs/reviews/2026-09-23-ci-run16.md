# CI run #16 实证记录（review/m2a0-3ccdf29 tag 触发）

> **最后更新**：2026-09-24 10:32
> **冻结 SHA**：`3ccdf29` (M2-A.0 本体)
> **触发**：push to tag `review/m2a0-3ccdf29`
> **结论**：整体 ❌ failure（backend job exit 1 — Ruff check 红），但 P0/P1 验证步骤 ✅
> **关联决策**：D-36-A/B/C + D-45 + D-39-X（末行自引用）
> **维护者**：作者冻结（不得 amend；D-39 第 5 条硬规则）

## §1 Brief（冻结点 + 范围 + 评估类别）

- **冻结 commit**：`3ccdf293b8b27b74454f83d68110dd97ef645784`
- **触发方式**：push to `review/m2a0-3ccdf29` (lightweight tag)
- **评估类别**：M2-A.0 准入前置 — 独立审查 (review5) 静态降级，CI 真跑作为机器证据

## §2 D-36-A 环境钉死

| 锚 | 值 | 来源 |
|---|---|---|
| commit SHA | `3ccdf29` | tag |
| Python | `3.11` (CI runner) | ci.yml L60-69 |
| 镜像 digest | 缺失 ⚠️ | runner 默认 image，未钉死 |
| 依赖版本 | 缺失 ⚠️ | `requirements.txt` 全部 `>=`（决策书 §四新-2 工程债）|
| PG 版本 | `12` | ci.yml 推断（默认）|

**D-36-A 完整度**：3/5 锚钉死（SHA + Python + PG），2/5 缺失（镜像 digest + 依赖版本）= 决策书 §六 "D-36-A 只钉了 SHA / Python / PG，没钉依赖版本"

## §3 八命令退出码（backend job 7 step + frontend 5 step）

### Backend job

| Step | 退出码 | 备注 |
|---|---|---|
| Setup Python 3.11 | ✅ | |
| Install Python deps | ✅ | |
| **Ruff check (app + tests)** | **❌ FAIL** | 6 lint errors in `backend/tests/test_review4_p11_fix_repro.py`（4 auto-fixable + 2 F811 重定义）|
| Mypy (app) | ✅ | B1 narrowing 在 py3.11 strict **不复现**（vs dev box py3.8 EXIT=1；归因实为 mypy 版本不锁，决策书 §四新-1 / §六）|
| Alembic upgrade (PG) | ✅ | 真迁移到 PG schema |
| Pytest (full run on PG) | ✅ | 含 `test_question_crud.py` D-37 负面测试（test_d37_fail_open_owner_filter_would_leak_data_returns_200 + test_d37_correct_owner_filter_returns_404_for_cross_user）|
| backend-up (uvicorn) | ✅ | uvicorn 启起来 |
| Verify end-to-end 5 chapters | ✅ | LLM 真值端到端验证 |

**Backend job 整体**：❌ failure（exit code 1 — Ruff 红拖垮）

### Frontend job

| Step | 退出码 |
|---|---|
| Setup Node 20 | ✅ |
| Install dependencies | ✅ |
| ESLint | ✅ |
| TypeScript check | ✅ |
| Build | ✅ |

**Frontend job 整体**：✅ success

### 总结

- **Backend**：6 绿 1 红
- **Frontend**：5 绿 0 红
- **总体**：❌ failure（Ruff 红拖垮 backend）

## §4 关键发现

### 4.1 decision book 硬-1/2/3 实测结果

| 决策书说法 | CI 实证 | 判定 |
|---|---|---|
| **硬-1**: review5 自我豁免 D-32 P0 第 3 类安全/归属边界 | Pytest ✅ 含两条 D-37 跨用户 404 负面测试 | ✅ **部分反证**：M2-A.0 P0 真有机器证据覆盖（pytest PG），但 review5 的"代码视觉 + 存在性"论证路径违规（D-45 已落字条）|
| **硬-2**: mypy 0 errors 是 dev box cache 漂移假绿 | CI mypy py3.11 fresh 跑 = **✅ 通过** | ✅ **完全反证**：M2-A.0 mypy 在 py3.11 strict 下真通过；B1 narrowing 归因 = mypy 版本不锁（决策书 §三修-1）|
| **硬-3**: M2-A.0 从未执行 | CI run #16 **首次真执行**：alembic upgrade + pytest + verify | ✅ **完全反证**：M2-A.0 已真执行且通过（除 Ruff lint）|

### 4.2 Ruff 失败细节

```
backend/tests/test_review4_p11_fix_repro.py:
  F401 [*] `tests.conftest.mock_db_engine` imported but unused (line 8:5)
  F401 [*] `tests.conftest.FIXTURE_NAME` imported but unused (line 9:5)
  F811 redefinition of unused `mock_db_engine` (line 12:8)
  F811 redefinition of unused `FIXTURE_NAME` (line 13:8)
  F541 f-string without any placeholders (line 41)
  F541 f-string without any placeholders (line 66)
6 errors, 4 fixable with --fix
```

**关键**：该文件在 216a0c1 已 `git rm` 删除（commit message 自陈）→ HEAD 不含此文件 → **HEAD 的 CI 跑 ruff 应 ✅**。

**origin-unknown 误报正解**（决策书 §二）：文件由 7ff30aa 引入（73 lines）、216a0c1 删除（73 deletions）。M2-A.0 仓自洽。

### 4.3 M2-A.0 仓自洽性证明

`git log --all --full-history -- backend/tests/test_review4_p11_fix_repro.py`：

```
216a0c1 test(review4): git rm repro test 文件（review4 SQLite 复现已归档到 review4 报告）
7ff30aa docs(retro): review3 + review4 brief/report 归档（D-39）+ review4 repro test
```

**M2-A.0 仓 origin = 7ff30aa（repro file 引入 commit）→ 216a0c1（repro file 删除 commit）→ HEAD clean**。自洽。

### 4.4 pytest PG vs dev box SQLite 的差异（解释 "20/20 SKIPPED → 真跑"）

`backend/tests/test_question_crud.py:42-46` 有 `pytestmark = _skip_on_py38`——dev box py3.8 跳过所有这些测试；CI py3.11 不触发 skip → 全部真跑 = PG fixture + integration tests + D-37 负面测试 ✅

## §5 M2-A.1 准入矩阵（决策书 §五 摘录 + 本 run #16 实证）

| 准入项 | 判定 | 依据 |
|---|---|---|
| 冻结基线三方一致 | ✅ | HEAD = `5e05b3a` 三方一致（local + origin 一致；GitHub ssh-agent transient gap）|
| 工作树干净 | ❌ | 3 modified 未提交（D-45 三文件）→ 决策室正在 commit |
| 迁移链可 apply | ✅（采信） | run #16 `alembic upgrade head` ✅；但见 §三新-1 alembic 工程债（35 字符 revision id + ci.yml:87-89 假陈述 + version_table_column_type 缺 + alembic 不锁）|
| 归属边界 P0 有机器证据 | ✅（采信） | pytest on PG 真跑，含两条 D-37 跨用户 404 负面测试 |
| mypy / 类型门禁 | ✅（采信） | CI py3.11 绿；dev box 的 EXIT=1 归因需按修-1 改写（mypy 版本不锁，非 py 版本差异）|
| 端到端 LLM 验证 | ✅（采信） | run #16 verify step 绿 |
| 独立审查结论有效 | ⚠️ | review5 = 静态降级 + 自我豁免，未按 D-45 第 3 条修订；M2-A.1 准入后需补修订 |
| 机器证据已落盘 | ❌ | 无任何 CI 日志归档 → 本文件即修复 |

## §6 D-39-X 自带出口检查

本文件 = `docs/reviews/2026-09-23-ci-run16.md`（机器证据落盘）。

---

**关联 baseline 文件**：`docs/reviews/2026-09-23-3ccdf29-baseline.md`（review5 frozen baseline）

**D-39-X 末行自引用**：

```
Baseline: docs/reviews/2026-09-23-3ccdf29-baseline.md (frozen) + docs/reviews/2026-09-23-ci-run16.md (machine evidence)
```

## §7 来源

收口决策书 2026-09-24 §三 / §四新-1 / §六 + GitHub Actions run #16（手动页面查看）+ git 实测（HEAD = 5e05b3a，三方一致；GitHub ssh-agent transient gap）+ 9/22/23 memory + decisions.md D-43/D-44/D-45。