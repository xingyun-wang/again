# review3 brief — M1-B retro review3（独立审查，D-44 路径）

> **冻结 SHA**：`fbb727559e66a58a22237a298fdd8620f163d02b` (M1-B retro P0-N1 + P1-5)
> **审查模式**：独立审查 subagent + D-33 中立
> **审查范围**：1 commit (fbb7275) + review2 baseline + 4 次 CI run evidence
> **关联决策**：D-44 (M2 解封路径重定) / D-43 (决策室不修代码逻辑) / D-42 (失败模式防护)
> **落地路径**：`docs/reviews/2026-09-21-fbb7275.md`（审查员产出报告）

---

## 1. 任务

**M1-B retro review3** — 独立审查 `fbb7275` commit（M1-B retro P0-N1 + P1-5），
基于 review2 baseline + CI 4 次 run evidence + D-32 判级标准，**判通过 / 不通过 M2 题库 CRUD 启动**。

**关键差异**（vs review2）：
- review2 冻结 `77095e6`（M1-B retro 工单 B merge commit）
- review3 冻结 `fbb7275`（M1-B retro P0-N1 + P1-5 commit）—— 跳过了 CI-1 + B1-1 越权 commit
- **审查范围更窄**：只看 1 commit（fbb7275），不审 8 commits
- **审查目的**：判 M2 题库 CRUD 启动门槛，不是判 CI 5 命令全绿（D-44 拍板）

---

## 2. 必读上下文（D-33 必读）

1. **产品定义**：`docs/产品定义-v0.5.md`
2. **最近 3 天日志**：`memory/2026-09-19.md` (跳过) / `memory/2026-09-20.md` / `memory/2026-09-21.md`
3. **进度**：`docs/STATE.md`
4. **宪法**：`docs/PROJECT-CHARTER.md`（含 §7.0 / §7.6 D-25/D-36/D-43 引用）
5. **决策**：`docs/decisions.md`（D-28 / D-29 / D-30 / D-31~D-39 + D-40 / D-41 / D-42 / D-43 / D-44）
6. **review2 报告**：`docs/reviews/2026-09-20-77095e6.md`
7. **review3 brief**：本文档

---

## 3. 审查范围（D-31 冻结 + D-32 判级）

### 3.1 冻结对象（1 commit）

- `fbb727559e66a58a22237a298fdd8620f163d02b` (M1-B retro P0-N1 + P1-5)

### 3.2 审查 commit 内容

- 5 文件 / +697 / -7
- `backend/app/api/v1/routers/academic.py`：subject_id 校验加 `_enforce_owner_or_404`
- `backend/app/services/textbook_upload.py`：service 层 defense-in-depth + 等分 fallback title 标记
- `backend/app/pdf/extractor.py`：新增 `is_template_title` helper
- `backend/tests/test_p0n1_subject_ownership.py`：D-37 fail-open 负面测试
- `backend/tests/test_p15_title_quality.py`：title 质量门槛测试

### 3.3 D-32 P0/P1/P2 判级标准（必须用这套）

**P0 命中任一即为 P0**：
1. **数据可信性**：写库内容与来源不符；对外结论（统计/验收/报表）可能失真
2. **不可逆损失**：删除原件 / 无备份覆盖写 / 迁移不可回滚
3. **安全与归属边界失效**：越权读写 / ID 存在性泄漏
4. **门槛 fail-open**：最坏实现（mock / fallback / 空壳）可让门槛亮绿
5. **单点可用性**：单个正常请求可导致服务不可用（无上限入内存 / OOM）

**P1**：不影响当前正确性，但在既定路线上造成返工或阻塞。
**P2**：整洁度、文档、命名、死代码。

**附加纪律**：
- 无法用一条命令复现的问题 → 降一级（逼证据落地）
- **fail-open 一律 P0**（本项目复发率最高的病，单列）
- 标准不改；但**判级可复议**（复议须书面，不改变本轮结论）

### 3.4 baseline 复核（D-34）

**review2 baseline B1-B5**（review3 不清，仅核对是否仍开）：

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| **B1** | `textbook_upload.py:443` mypy narrowing 错 | 仍开（fbb7275 未修） | `cd backend && mypy app` → 1 error |
| **B2** | dev box pytest collect errors | 仍开 | `pytest tests --collect-only -q` → 9 errors（py3.8 + psycopg 缺） |
| **B3** | CI 闸门（含 PG fixture + 集成测试） | 已配置（CI-1 commit 8d3e3be 后续被回滚，但 fbb7275 不含） | **fbb7275 不含 CI 配置**，B3 与 fbb7275 无关 |
| **B4** | pyproject.toml disable_error_code 无效 | 仍开 | **fbb7275 不动 pyproject.toml**，B4 与 fbb7275 无关 |
| **B5** | dev box alembic + 真 LLM | 仍开 | **fbb7275 不动 alembic**，B5 与 fbb7275 无关 |

**review3 baseline 增量**（fbb7275 引入的）：

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| **B6** | P0-N1 subject 归属校验实现正确性 | 待审 | 检查 routers/academic.py + service 层 |
| **B7** | P1-5 title 质量门槛实现正确性 | 待审 | 检查 extractor.py + service 层 |
| **B8** | D-37 负面测试可证伪 | 待审 | 检查 test_p0n1_subject_ownership.py fail-open 测试 |
| **B9** | 测试覆盖度（integration / unit） | 待审 | pytest --collect 数量对比 review2 baseline |

### 3.5 D-36 证据要求（真实执行产出）

**审查材料必须含三锚**（D-36-A/B/C）：

1. **D-36-A 环境钉死**：commit SHA + Python 3.8（dev box baseline）/ Python 3.11（CI runner）+ 镜像 digest
2. **D-36-B 机器证据**：命令原文 + 退出码（不得摘要 / 剪辑）
3. **D-36-C 降级声明**：dev box 无 docker 起 backend = 静态审查模式（与 review2 一致）

### 3.6 CI 4 次 run evidence（D-44 重定路径）

| run | commit | status | conclusion | 关键 fail step |
|---|---|---|---|---|
| #1 | fbb7275 | completed | failure | step 5 Install Python deps（缺 working-directory）|
| #2 | 3c21dd1 | completed | failure | step 7 Mypy（B1 narrowing）|
| #3 | 171aabe | completed | failure | 未深入诊断（C 路径触发）|
| **#4** | **fbb7275**（force push 后）| completed | failure | **同 #1**（step 5 working-directory）|

**关键事实**：
- CI 4 次 run 全部 failure，**但与 fbb7275 commit 内容无关**——都是 ci.yml 配置问题
- fbb7275 commit 本身在 dev box 跑 ruff/mypy/pytest helper 4 闸门全过
- **CI 5 命令全绿 ≠ M2 解封（D-44 拍板）**——M2 解封靠 review3 独立审查判通过

---

## 4. 输出格式

### 4.1 报告头部（D-31 冻结一致）

- 第 1 行：`git rev-parse HEAD` 输出（必须 = `fbb727559e66a58a22237a298fdd8620f163d02b`，否则作废）
- 第 2 行：审查模式（实跑 / 静态）
- 第 3 行：审查范围列出 commit SHA 列表

### 4.2 报告主体

- **D-34 baseline 状态复核**（B1-B9）
- **新发现 P0/P1/P2 清单**（每条：编号 + 严重程度 + 文件:行号 + 证据 + 修复建议）
- **通过 / 不通过判定**（M2 题库 CRUD 启动门槛）
- **豁免记录**（D-35 五要素 + 审查员附署 — 本轮不允许豁免 P0 中安全/归属边界与数据可信性两类）

### 4.3 报告落地路径

- `docs/reviews/2026-09-21-fbb7275.md`（review3 报告）

---

## 5. 硬规则

1. **D-31 冻结一致**：报告第 1 行 SHA 必须 = `fbb727559e66a58a22237a298fdd8620f163d02b`，不一致 → 报告作废
2. **D-33 中立**：不接受 brief 之外的「作者结论」；commit message 免责（D-33 第 3 条）
3. **D-36 真实证据**：禁止「已通过 / 已修 / 可审」等无退出码措辞（除非贴有命令原文）
4. **D-35 豁免**：本轮**不允许豁免 P0**（安全/归属边界 + 数据可信性）；其他 P0/P1/P2 豁免需五要素 + 审查员附署

---

## 6. 不要做（边界严格只读）

- 不要修改任何文件
- 不要 `git push` / `git tag` / `git merge` / `git rebase` / `git commit`
- 不要看主会话我之前的判定表 / P0-N1/P1-5 工单交付报告（D-33 中立：审查员是全新视角）
- 不要信任 brief 之外的任何「作者结论」

---

## 7. 技术约束

- `pwd` 应是 `/home/wsq_1/tiered-homework-platform`
- Python 3.11 (prod / CI) / 3.8 (dev box)
- dev box 无 docker 起 backend（thp-backend 是 M0 时期旧镜像，路径布局与新代码不一致）
- pytest 跑不动时改用 collect-only，不要谎称全过

---

## 8. 回报

- 报告落地 = 你的最终输出（这条 review session 的最后一条消息）
- 报告标题首行：`git rev-parse HEAD` 输出
- 报告末行：明列 P0/P1/P2 数量 + 通过/不通过判定
- 报告在主会话可见（不要 commit；直接 send）
