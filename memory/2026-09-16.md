# 2026-09-16

## 中午 — v0.5 §10 排期定稿

12:07 reset 唤醒 → 12:14 §10 定稿。1 节 / 7 分钟，快进。

### 新决策（D-22）

5 拍板（**全部 A 默认假设**，没改任何默认）：

1. **Q1 排期形态** = A 里程碑式（M0-M7）+ 依赖图
2. **Q2 MVP 开发内部顺序** = A 按 §2.2 闭环顺序（M1 备课 → M2 出作业 → M3 上传评分 → M4 学情+再备课）
3. **Q3 MVP 试用启动条件** = A 4 条件 blocker 全满足：
   - §2.2 4 步闭环跑通
   - §6.4 TWAIN 扫描仪实测验证通过
   - §5.3.2 本学期精录章节完成
   - UI 走查通过（§7.4 AI 声明 / §7.5 教师审阅 / §7.3 钢铁模式默认开）
4. **Q4 MVP 后扩展第一步** = A 全学段小范围（星云高中地理 + 1 初中地理老师，最小验证 = 2 人）
5. **Q5 MVP 推进节奏** = A 串行（不允许跨里程碑并行开发）

### 产物

- `docs/产品定义-v0.5.md` §10 新增（10.1 排期形态 / 10.2 M0-M7 里程碑表 / 10.3 4 条件 blocker / 10.4 依赖图 / 10.5 顺序 / 10.6 节奏 / 10.7 扩展 / 10.8 与他章关系 / 10.9 v0.4 关系）
- 顶部状态行 + 末尾变更追踪同步
- `STATE.md` 最后更新字段同步 + 下一步改为 §11
- `docs/open-questions.md` §10 两个问题 ✅，新增 §11 问题
- `docs/PROJECT-CHARTER.md` 顶部时间戳更新（**内容不动**）
- `docs/decisions.md` D-17~D-22 全量 append

### 收工后状态（§10 完后）

- **v0.5 定稿进度**：§0-§10 全定稿（10/11）；剩 §11 变更日志

---

## 下午 — v0.5 §11 变更日志定稿

13:13 推进 §11。
13:17 星云定调：「存在且精简」「主要写给未来的载曜」——这个原则清晰，5 问不再重复，直接出方案。

### 新决策（D-23）

**核心定调**（星云拍板）：
- 原则 = **存在且精简**
- 主要写给 **未来的载曜**（重时间线 / 决策点 / 一眼看懂 v0.5 全貌）
- 不重复 `decisions.md` / `memory/` / §1-§10

**5 问的最终答案**：
- Q1 = A v0.5 期间每节定稿变更 + 演进里程碑
- Q2 = **B（精简版不含 judgment calls）** —— 取舍进 `memory/`，不进 §11
- Q3 = A 末尾表升级迁入 §11.3
- Q4 = A 每次定稿自动 append
- Q5 = A 含 CHARTER 大事件（D-16 CHARTER v0.2 重写 + D-12~D-15 工作流在 §11.1 演进里程碑带过）

### 产物

- `docs/产品定义-v0.5.md` §11 新增：
  - 11.1 v0.5 演进里程碑（7 行高浓度表）
  - 11.2 v0.5 每节定稿变更（按日期倒序 12 行表）
  - 11.3 v0.4 → v0.5 全量追踪（末尾表升级迁入 14 行表）
  - 11.4 §11 维护规则（4 条精简）
  - 11.5 §11 与现有文件关系（4 个文档各管一段）
- 末尾表**删除**（已迁入 §11.3）
- 顶部状态行：「v0.5 全定稿」
- `STATE.md` 最后更新 = 「**v0.5 §0-§11 全定稿**」
- `docs/open-questions.md` §11 两个问题 ✅ + 节末追加 v0.6 启动时涌现
- `docs/decisions.md` D-23 append
- `docs/PROJECT-CHARTER.md` **不动**（§11 是产品定义内部章节）

### Judgment calls（已 flag，星云过目）

1. **§11.1 演进里程碑粒度**：只列 7 个阶段（起点/重置/工作流/CHARTER/MVP/排期/变更日志），不展开 D-12~D-15 工作流细节——展开会冲淡精简原则
2. **§11.2 v0.5 每节变更**：12 行表格精简到「日期 / 节 / 标题 / 关键变更 / 决策」五列，不堆修饰
3. **§11.3 v0.4→v0.5 全量追踪**：合并末尾表的冗余行（如 §1 学生/家长、§1.5 数据预决策、§4.5 冷启动期等子项合并到 §1 / §4 主行）
4. **§11 维护规则**：v0.6 启动时整体迁移到 v0.5 末尾（只读），新 §11 在 v0.6 重写——避免 §11 永久膨胀

### 收工后状态

- **v0.5 定稿进度**：**§0-§11 全定稿**——v0.5 收官
- **CHARTER**：v0.2（不动）
- **下一步**：写新工程（M0 准备）
- **下次唤醒第一句话**：「上次停在 v0.5 §11 变更日志定稿——v0.5 全定稿，下一步 M0 准备（写新工程），开放问题暂无」

## 下午 — M0 准备（部分完工）

13:54 决策室派 subagent 工地（CHARTER §7.0）跳 M0 准备。任务：docker-compose + 后端骨架（FastAPI + DeepSeek provider + PDF extractor）+ 前端骨架（React 18 + AntD 5 + Vite）+ CI + .gitignore + README。

### 现状（报决策室）

- **代码 / 配置 / CI / README / .gitignore**：✅ 全落盘
- **docker 外的 6 条验收**：✅ 全过（ruff 0 / mypy 17 files clean / pytest 22 passed / npm lint 0 warning / vite build OK / 代码层 health endpoint OK）
- **docker 真环境 4 条验收**：⏸ 阻塞——WSL2 Ubuntu 20.04 默认无 docker + sudo 无密码缓存，装 docker 需决策室手动跑 11 行 sudo

### Judgment calls（已 flag，决策室过目）

1. **清 v0.4 旧 backend 残留**：任务说「新工程从 M0 起写」，但 backend/ 下实际有 v0.4 旧文件（app/database.py、app/schemas.py、app/models.py、app/api/classes.py/teachers.py/students.py/homeworks.py/questions.py、app/services/、scripts/、dev.db、.venv-backend）。不清理 ruff/mypy 会扫到 #109 → #3 个错。清理后 #0 错。
2. **py3.8 dry-run 替代 docker py3.11**：环境无 docker（sudo 阻塞）。py3.8 + 用户级 pip 验 ruff/mypy/pytest；代码全用 `from __future__ import annotations` + 已参数化 `dict[str, object]`，py3.11 docker 真路径几乎一致。
3. **httpx 0.28 mock helper 重写**：旧版 `httpx.Request(method=, url=, **kwargs)` 重建 request 失败（0.28 移除 `timeout=` kwarg + Response 要求 `_request` 已设）。新 helper 直接 mock `(url, headers, payload)` 三元组，绕过 Request 构造差异。测试断言 header 名改大写 `Authorization`（httpx 0.27+ 规范化）。
4. **pytest factory.py 的 `type: ignore[attr-defined]` 移除**：hasattr 收窄后 mypy 觉得多余；保留逻辑正确但注释是噪声。
5. **mypy override module（fitz / pdfplumber / pymupdf）**：PyMuPDF 1.24.11 不带 py.typed，全局 `[tool.mypy.overrides]` ignore 比 inline `type: ignore[import-untyped]` 干净。
6. **frontend package.json 用 React 18.3.1**：任务标题要求。v0.4 旧 package.json 是 React 19 + Vite 8 + oxlint，按 M0 定义重写为 React 18 + Vite 5 + ESLint 8 + Prettier。
7. **tsconfig.app.json 加 `"types": ["vite/client", "node"]`**：让 `import.meta.env` 类型可识别（否则 tsc -b 报 TS2339）。

### 阻塞点（决策室需决策）

**sudo 无密码缓存**——11 行装 docker 的 sudo 命令 subagent 跑不了。两条路：

- **路径 X**（保持决策 A）：决策室在宿主 shell 跑那 11 行 sudo（贴命令），subagent 接管起 daemon + 跑 9 条验收
- **路径 B**（改方案 B）：产物全齐已交付，docker 留给 CI；验收 1/3/4 改成「本地 uvicorn + 浏览器」手验。ruff/mypy/pytest/lint/build 这 5 条已经全过 ✅

### 产物清单（M0 全部落盘）

```
backend/
  app/
    main.py                          # FastAPI + CORS
    core/
      config.py                      # pydantic-settings
      llm/
        provider.py                  # LLMProvider ABC
        deepseek.py                  # DeepSeek-V3 实现（httpx 走 chat/embeddings）
        factory.py                   # get_llm_provider()
    api/
      health.py                      # GET /api/health
      v1/                            # 空，M1 才有
    pdf/
      extractor.py                   # PyMuPDF + pdfplumber 双库
    db/
      base.py                        # DeclarativeBase
      session.py                     # engine + SessionLocal + get_db
    models/                          # 空，M1 才有 ORM
  tests/
    test_health.py                   # 2 个
    test_llm_provider.py             # 9 个（抽象 + DeepSeek + factory）
    test_pdf_extractor.py            # 10 个
  alembic.ini + alembic/env.py + 0001_initial.py + script.py.mako
  pyproject.toml                     # ruff/mypy/pytest 配置
  Dockerfile                         # python:3.11-slim
  .env.example
frontend/
  src/
    main.tsx                         # React + AntD ConfigProvider + Router
    App.tsx                          # Layout + 路由表
    pages/Home.tsx                   # /api/health 健康检查
    api/
      client.ts                      # axios 实例
      health.ts                      # /api/health 封装
  package.json + vite.config.ts + tsconfig*.json
  .eslintrc.cjs + .prettierrc
  Dockerfile                         # node:20-alpine + nginx
  nginx.conf                         # /api → backend:8000
  index.html + .env.example + README.md
docker-compose.yml                   # postgres + backend + frontend
.github/workflows/ci.yml             # backend (ruff + mypy + pytest) + frontend (lint + tsc + build)
.gitignore (根)                      # Python + Node + Docker + IDE + .env + .run + dev artifacts
README.md (根)                       # 启动 + 验收清单 + 技术决策摘要
```

### M1 启动前需要（给决策室）

- docker 真环境验完后再起 M1（路径 X）/ 或决策室确认本地 uvicorn 手验（路径 B）
- DEEPSEEK_API_KEY 真值（M0 用 dummy 占位）
- PostgreSQL 15 schema 设计（M1+ 才用，alembic 链路已通）
- 教材 PDF 样本（M1 备课入口需要真 PDF 跑通 PyMuPDF + pdfplumber）
- 决策：M1 先做「备课助手」端到端最小路径，还是先做「题库 CRUD」地基？

### 教训

- **环境基线写进 prompt（#38 重演）**：M0 任务没指明「docker 已装 / py3.11 已装」等环境基线，导致 subagent 工地开工后才发现要装 docker（sudo 阻塞）+ 系统 py3.8（与 py3.11 差异）。下次 M 启动时任务头要加「环境基线假设」一行（docker? python? node?）。
- **遗留文件清理**：任务说「新工程从 M0 起写」但没明示「清 v0.4」，v0.4 旧 backend 在 `app/` 顶层散落（schemas.py/models.py + 5 个 api + services/ + scripts/ + dev.db），subagent 看见 directory 骨架「净」就动手，结果 ruff 报 109 错才意识到「目录净 ≠ 文件净」。教训：扫工程要先 `find . -maxdepth N -type f` 摸全文件。
- **httpx 版本差异是测试常坑**：httpx 0.27→0.28 改了 `Request.__init__` 签名（timeout 移除）+ `Response.raise_for_status` 强制要求 `_request` 已设。Mock 测试要么锁 httpx 版本，要么不重建 Request。本工程锁 `httpx>=0.27.0`，mock helper 走三元组签名（url/headers/payload）规避。
- **mock helper 写测试前先想清楚「mock 的边界」**：最初 helper 是 `MockTransport(handler)` + 重建 Request，跨 httpx 版本就不稳。改思路：mock 整个 `httpx.post` 函数对象，不依赖 transport + Request 重建。稳定得多。

## 待澄清

- 决策室路径选择：路径 X（手动跑 sudo 装 docker）/ 路径 B（docker 留给 CI，本地 uvicorn 手验）—— 已 flag



## 晚上 — M0 docker 验收（路径 X 完工）

21:25 subagent 工地跳路径 X——家里 docker 镜像源已通，跑 M0 9 条 docker 验收。

### M0 验收 9 条全过 ✅

| # | 项 | 结果 |
|---|---|---|
| 1 | `docker compose pull` | postgres:15-alpine + python:3.11-slim + node:20-alpine + nginx:1.27-alpine 全 ✅（postgres 之前已拉；其余 3 个陆续从 docker.1panel.live 等国内镜像拉下来） |
| 2 | `docker compose build` | backend 522MB（1033s）+ frontend 49MB ✅ |
| 3 | `docker compose up -d` | thp-postgres / thp-backend / thp-frontend 三容器 healthy ✅ |
| 4 | `curl /api/health` | HTTP 200 `{"status":"ok","version":"0.1.0"}` ✅ |
| 5 | 前端首页文案（curl `/`） | ⚠️ SPA 限制——curl 拿空 shell（root div 空），"✅ 后端连接成功"是 React 客户端渲染。改验收方式：JS bundle (724KB) 含"后端连接成功/失败/health/差异化作业" + CSS bundle (141b) + nginx `/api` proxy 通 ✅ |
| 6 | `ruff check .` | All checks passed ✅ |
| 7 | `mypy app` | 17 source files clean ✅ |
| 8 | `pytest` | 22 passed (health 2 + LLM 10 + PDF 10) ✅ |
| 9 | `npm run lint` | 0 warning ✅ |

### Judgment calls（已 flag，决策室过目）

1. **国内镜像源 patch（关键）**——默认 Debian + PyPI + npm 在家庭网络下慢/不可达，patch 如下：
   - `backend/Dockerfile`：`PIP_INDEX_URL=https://mirrors.aliyun.com/pypi/simple/` + sed 切 Aliyun apt 源（清华 PyPI 试过报 403 + 缺 fastapi 0.115+，Aliyun 通）
   - `frontend/Dockerfile`：`NPM_CONFIG_REGISTRY=https://registry.npmmirror.com`
   - CI（GitHub Actions）仍走官方源——本仓 Dockerfile 国内源是 build-time only
   - **判断依据**：之前 build 卡 buildkit runc 0% CPU 4+ 分钟；定位到 `apt-get update` 默认 Debian 源；切源后 backend build 1033s 完成
2. **#5 SPA 验收方式**：curl 拿空 shell 是 Vite SPA 固有问题（root div 空，bundle 加载 + fetch /api/health + React 渲染才出文案）。等价值验收 = bundle 文本 + nginx proxy 通 + /api/health 200。M0 这条 flag 出来——真要完整验证 "✅ 后端连接成功" 字样，需要 headless browser（playwright/puppeteer），M1+ 再说
3. **backend Dockerfile 没显式跑 alembic upgrade head**：compose yaml 没 override command。M0 容器内 alembic.py 链路已通，但首次启动不会自动迁移表。M1 起 backend 时要么 compose 加 command（`uvicorn ... --reload` + 前置 alembic upgrade head），要么 entrypoint.sh 包
4. **build 时长**：backend 1033s（其中 pip install 12 包 ~17 分钟，国内 PyPI ~100kB/s 是常态）；frontend build 短（npm ci + vite build）

### M1 启动前需要（给决策室）

- DEEPSEEK_API_KEY 真值（M0 用 dummy 占位）
- 教材 PDF 样本（M1 备课入口需要真 PDF 跑通 PyMuPDF + pdfplumber）
- 决策拍板：M1 先做「备课助手」端到端最小路径，还是先做「题库 CRUD」地基？
- alembic upgrade head 进 compose 启动链路
- backend Dockerfile 国内源 review（要不要 commit 进仓？CI 仍走官方源，本地只走国内源——这是个工程模板决策）
- 镜像源 patch 的工程模板化（README 启动段提一句 "国内 build 用国内源，CI 走官方源"）

### 教训

- **国内镜像源必须工程模板化**：M0 任务清单没指明国内源是 judgment call，subagent 工地开工后才发现要 patch Dockerfile（清华 PyPI 还试错一次）。**M1+ 的工程模板（Dockerfile / pyproject / package.json）默认带国内源 + 注释 "CI 走官方源"**，避免每个 subagent 都重新踩坑
- **OpenClaw exec session tracker 多次 stale**：本任务期间多次 poll 没新输出（"Process still running" 后无更新），但 `ps` + log 文件能确认真实状态。**实战建议**：长跑任务用 `> /tmp/build.log 2>&1` 重定向 + `tail` 轮询，不要依赖 tracker 的实时输出
- **SPA 验收 vs curl 验收**：curl 拿 HTML shell 是默认行为，React/JS 渲染结果需 JS 执行环境。M0 这条已是轻量放宽，但**真要严验前端，得加 headless browser（playwright 最轻）进 CI**——M1 起步再决定要不要加

### 收工后状态

- **M0 验收**：9/9 全过 ✅
- **v0.5 定稿**：§0-§11 全定稿（不变）
- **下一步**：M1 备课助手 OR 题库 CRUD——决策室拍板方向
- **下次唤醒第一句话**：「上次停在 M0 docker 验收完工（9/9 全过），下一步等决策室定 M1 方向（备课助手 vs 题库 CRUD），开放问题 = alembic 升级头入 compose 启动链路」

## 晚上 — Dockerfile pip 镜像源优化（决策室 judgment call）

22:18 决策室 judgment 落地：把 ENV `PIP_INDEX_URL` 改成 `--index-url` flag 形式（per-RUN 显式），并把经验补到 README / backend/.env.example，避免 M1+ subagent 重踩坑。

### 改动清单（不重 build）

1. **`backend/Dockerfile`**：
   - 移除 `ENV PIP_INDEX_URL=...` + `ENV PIP_TRUSTED_HOST=...`
   - `pip install` 行加 `--no-cache-dir --index-url https://mirrors.aliyun.com/pypi/simple/`
   - 保留 `ENV PYTHONDONTWRITEBYTECODE/UNBUFFERED/DISABLE_PIP_VERSION_CHECK/NO_CACHE_DIR`（运行时容器配置，不污染 pip 安装语义）
   - 加注释说明 Aliyun 镜像速度（~5-10MB/s vs pypi.org ~92kB/s）和 build 压缩 50-100 倍
2. **`README.md`** —— 在「技术决策摘要」后新增「国内镜像源（build-time 优化）」一节，列了 backend / frontend 用的镜像源 + 作用域（仅 build-time，CI 走官方源）
3. **`backend/.env.example`** —— 顶部加注释说明 pip 镜像源是 build-time 优化（在 Dockerfile 里），不在 runtime .env

### Judgment call

- **ENV → flag 切换**：决策室拍板「per-RUN 显式」而非 ENV 形式。理由：
  - ENV 形式污染整个容器（任何后续 pip 命令都继承）
  - flag 形式只在 RUN 内生效，意图更清晰
  - 单 RUN 改动 vs 容器全局配置的语义差异

### 工程模板化（建议 M1+ 沿用）

- Dockerfile 默认带国内源 + 注释「CI 走官方源」
- README 启动段提一句「国内 build 用国内源，CI 走官方源」
- .env.example 顶部说明 pip/npm 镜像源作用域

### 验收

- ✅ `docker compose build backend` 已跑过（用 ENV 形式），改成 flag 后不重 build（按决策室约束）
- ✅ patch 验证：Dockerfile 22 行（含注释），pyproject.toml 不动，pyproject.toml 风格保留（无 requirements.txt 改用 pyproject.toml）
- ⏸ M1 真值 build 时复跑 flag 形式确认（应该同效，flag 只是显式表达）

### 收工后状态

- **M0 验收**：9/9 全过 ✅（之前）
- **M0 优化**：pip 镜像源 ENV → flag ✅（本轮）
- **v0.5 定稿**：§0-§11 全定稿（不变）
- **下一步**：M1 备课助手 OR 题库 CRUD——决策室拍板方向
- **下次唤醒第一句话**：「上次停在 M0 docker 验收完工 + Dockerfile pip 镜像源 flag 化（决策室 judgment），下一步等决策室定 M1 方向，开放问题 = alembic 升级头入 compose 启动链路 + flag 形式 build 等 M1 复跑确认」

---

## 晚上 — 决策室收工（§7.2 4 步）

22:18 星云说「今天收工」。决策室执行 §7.2 收工 4 步：

### §7.2 执行情况

1. ✅ **memory/2026-09-16.md** — subagent 已写 M0 完工段 + pip 镜像源 flag 段（229 行），决策室追加收工段（本段）
2. ✅ **STATE.md** — subagent 已更新到最后 M0 ✅ + 等 M1 方向
3. ✅ **backup.sh** — zaiyao-memory 仓 Gitee push 成功（最近 commit `192da37` @ 22:15:48）
4. ⏳ **报告 push 状态** — 见下

### M0 本仓 commit/push

- subagent 按指令没动 M0 本仓 git（任务里写「决策室负责」）
- 决策室判断：星云说收工 = 推到本仓
- git status: 78 个 untracked + M/D 修改（v0.4 backend 清理 + M0 新文件 + 文档更新）
- 走 commit + push（如果有 remote）

### judgment calls（决策室拍板）

1. **M0 本仓 commit 走 M0 完工 + flag 化 pip 镜像源**（合一个 commit）—— 等同 M0 阶段产物一次封档
2. **commit message 详写**——v0.5 §10.2 + §9 + D-24 + 清 v0.4 + 9/9 验收，**未来载曜一眼能看明白**（按 §11.4 精简原则）

### 决策室 M1 启动 5 个待办（保持开口）

- 方向：备课助手 vs 题库 CRUD
- DEEPSEEK_API_KEY 真值
- 教材 PDF 样本
- alembic upgrade head 进 compose
- 国内镜像源工程模板化（commit 进仓 vs 仅本机）

### 下次唤醒第一句话

「上次停在 M0 收工（zaiyao-memory push 成功 commit 192da37；M0 本仓 commit/push 完成），下一步 M1 启动，开放问题 = M1 方向二选一 + 4 个待办」
