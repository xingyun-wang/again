# memory/2026-09-24.md — 收口决策书接住 + D-43-X 落字 + M2-A.1 准入待 P4

> **最后更新**：2026-09-24 10:32
> **作者**：决策室主会话

## 一、今日流程（按时序）

### 1.1 收口决策书 §一/二/三/四/五/六 接住

- **§一 直答**：可开 M2-A.1（采纳）
- **§二 path 修正**：决策室之前"origin-unknown"是路径写错的误报（漏 `backend/` 前缀）
- **§三 3 修正 + 4 新发现**：全部接受（修-1/2/3 + 新-1/2/3/4）
- **§四 5 动作**：1+3+4+5 采纳，2 撤销（"fix ruff"幽灵工单——HEAD 没文件 216a0c1 已删）
- **§五 准入表**：2 项 ❌（工作树 + 机器证据落盘）→ 动作 1+3 各修一项
- **§六 总结**：采纳率 ~90%，核心修正 = fix ruff 是幽灵 + origin-unknown 是路径错 + mypy 归因不是 py 版本而是 dep 不锁

### 1.2 立刻补执行的决策室工作（本轮）

- 修 STATE.md 首行 SHA 6b9f0a4 → 5e05b3a（连续第 4 次 STATE 首行 SHA 不符修复——新-4 工程债登记）
- append D-43-X 到 decisions.md（结论性陈述必须自我验证——§二 教训，第 3 次同型事故触发）
- 加 D-43-X bullet 到 AGENTS.md §代码审查纪律 段
- 创建本 memory（决策室记账）
- 创建 docs/reviews/2026-09-23-ci-run16.md（run #16 机器证据落盘 = D-39 第 3 条首次活实践）

### 1.3 等 P4（HEAD CI run 验）

- HEAD = 5e05b3a 已 push 双 remote（local + origin 一致；GitHub ssh-agent transient gap，用户已访问 Actions page）
- P4 = HEAD 的 CI run 应 backend 全绿（repro file 已删）+ frontend 全绿 = overall success → M2-A.1 准入 ✅
- 已给用户详细手动步骤（GitHub Actions page 看 ruff/mypy/alembic/pytest/verify 五命令退出码）

## 二、关键技术判断

### 2.1 D-43-X 候选规则（同型事故第 3 次触发）

| 同型事故 | 命令 | 误报结论 | 正确结论 |
|---|---|---|---|
| #1 | （run 命名） | F949E91 死 SHA | 实际 commit 在 reflog 中 |
| #2 | `ls ~/zaiyao-memory/backup.sh`（dev box 路径未展开）| backup.sh 不存在 | 实际存在，路径错 |
| #3 | `git log --all -- tests/test_review4_p11_fix_repro.py`（漏 `backend/` 前缀） | M2-A.0 仓 origin-unknown / 不自洽 | 实际 `git log --all -- backend/tests/test_review4_p11_fix_repro.py` 有 216a0c1 + 7ff30aa，仓自洽 |

**D-43-X 落字**：任何"异常/缺失"结论必须自我验证三步：
1. 完整原命令（不省略路径前缀 / 限定词 / 文件名通配符）
2. 独立路径形式复跑一次（grep + ls-tree + log + show + status 五源至少二源）
3. 失败原因验证（区分路径错 vs 确实缺失）

### 2.2 STATE.md 头部 SHA 问题的根治（新-4）

- **临时修**：手动改 SHA 到 5e05b3a
- **根治方案**（新-4 工程债）：commit-hook 自动更新 STATE 首行 SHA → commit subject 形式（不写 SHA）
- D-43-X 与新-4 同根：决策室状态陈述依赖派生事实，需要 auto-sync 闭环

### 2.3 mypy 差异归因（修-1）

- **错误归因**：dev box py3.8 vs CI py3.11（pyproject.toml `python_version = "3.11"` 对所有 runner 生效）
- **真归因**：mypy 版本不锁（1.13 → 1.15 partial-type inference 行为变化）
- 决策书 §四新-1 给了同根解释（alembic 35 char + dep 不锁）
- 决策书 §六明确："D-36-A 只钉了 SHA / Python / PG，没钉依赖版本"
- **工程债**：D-36-A 加第四锚（依赖版本指纹）+ 锁文件

## 三、新发现 / 已知问题（待 M2-A.1 启动后处理）

### 3.1 新-1 alembic 工程债（登记 M2-A.1 首个工单）

- `0005_drop_extraction_source_default` = 35 chars（超 alembic 默认 VARCHAR(32)）
- `ci.yml:87-89` 假陈述"迁移 ID 字符数回到 32 以内"
- `backend/alembic/env.py` 缺 `version_table_column_type` 配置
- `alembic>=1.14.0` 不锁（依赖漂移 / 升级风险）
- **优先级**：不阻塞 M2-A.1 启动；登记 M2-A.1 首个工单

### 3.2 新-2 D-36-A 第四锚工程债

- 当前 D-36-A 3/5 锚钉死：SHA + Python + PG；缺镜像 digest + 依赖版本
- 决策书 §六："D-36-A 只钉了 SHA / Python / PG，没钉依赖版本"
- 修复：lock 文件 + `pip freeze` hash打印进 verify step 的 D-36-A 段
- **优先级**：长期工程债

### 3.3 新-3 review5 §6.1 D-45 自检失效

- D-45 第 3 条要求 review 报告判定与 baseline 一致时必须修订或标失效
- review5 §6.1 仍写 "✅ B1 baseline 改善（mypy 0 errors）— 不构成新 P0"
- regen-baseline.md B1 已标 "review5 = 0 errors cache漂移但项未关闭"
- D-45 落地即失效（review5 是 D-45 之前写的，落地后 review5 §6.1 未修订）
- **优先级**：M2-A.1 准入硬性达标后立即修订 review5 §6.1

### 3.4 新-4 STATE.md 头部 SHA 自动刷新

- 连续 4 次 STATE 首行 SHA 与真实 HEAD 不符
- 根治方案：commit-hook 自动更新 STATE 首行 → commit subject 形式（不写 SHA）
- **优先级**：长期工程债（commit hook 实现 / 不写 SHA 改写）

### 3.5 P4（HEAD CI run 验）等用户

- HEAD = 5e05b3a 已 push 双 remote
- 应触发新 CI run（#17 或更高）
- 用户已给出详细手动步骤

## 四、下一步（要星云拍）

| 选项 | 含义 |
|---|---|
| **A** | HEAD 触发新 CI run（repro file 已删 = ruff 应 ✅）= 真验证 M2-A.0 端到端 |
| **B** | 接受 alembic/pytest/verify 仍跑不通的事实 = M2-A.1 **不能开**，必须先修 CI |
| **C** | 接受当前 + HEAD 跳过 alembic/pytest/verify（改“准入”为“HEAD CI ruff ✅”单点）|

**载曜建议 A。理由**：P4 验是 5min 工作，避免决策书 §三新-1 那种“实质条件 ❌ 形式条件 ✅”陷阱。

## 五、教训（D-43-X 候选）

详见 §二 2.1 同型事故表 + D-43-X 落字条（`docs/decisions.md`）。

### 同型事故序列（第 3 次触发）

1. **2026-09-21 #1**: F949E91 死 SHA 命名——run 命名 vs 实际 commit
2. **2026-09-23 #2**: "backup.sh 不存在"误报——`ls ~/zaiyao-memory/backup.sh` 路径未展开
3. **2026-09-24 #3**: "M2-A.0 仓 origin-unknown"——`git log --all -- tests/...` 漏 `backend/` 前缀

**D-43-X 落字条**（详细）：见 `docs/decisions.md` D-43-X 段。

## 六、来源

收口决策书 2026-09-24 §一/二/三/四/五/六 + git 实测（HEAD = 5e05b3a，三方一致；origin (Gitee) 一致；GitHub ssh-agent transient gap，用户已知）+ 9/22/23 memory + decisions.md D-43/D-44/D-45。