# 2026-09-15

## 上午 — CHARTER §7.0 加会话分工规则

10:25 星云要在 CHARTER 加会话分工规则。

### 新决策（D-17）

- **§7.0 会话分工**：
  - 主聊天界面（万物）= 决策室
  - 工地 = subagent / 外派 session
  - 写前后端 / 跑代码 / debug 在工地，避免上下文超长
- **违反警告**：长上下文调试 / 大段代码贴进决策室 = 反模式，载曜主动拦截
- **与 D-01 关系**：D-01（2026-09-11）初版规则；D-17 宪法级沉淀

### 产物

- `docs/PROJECT-CHARTER.md` §7.0 新增（顶部时间戳同步）
- `STATE.md` 最后更新字段同步
- `docs/decisions.md` D-17 append

---

## 上午 → 下午 — v0.5 §6-§9 定稿

### §6 数据采集定稿（D-18）

10:53-12:42

5 拍板：
1. Q1 采集路径 = **A** 只走 OMR（推翻 v0.5 §2.2 + CHARTER §2 默认「OMR 主 + OCR 备」）
2. Q2 答题卡格式 = A 系统标准模板（统一印刷）
3. Q3 主观题录分 = 按学生顺序手动录（MVP 极简 UI）
4. Q4 TWAIN 兼容性 = C MVP 前实测验证至少 1 款（**最大 blocker**）
5. Q5 数据存储 = C 本地为主 + 云端备份（脱敏后）

**CHARTER §2 同步改**：OCR 措辞改 → 「OMR 主路径；OCR 接口预留，**MVP 不做 UI**」

### §7 合规边界定稿（D-19）

13:25-13:33

5 拍板（全部沿用默认）：
1. Q1 家长授权 = A MVP 一次性授权
2. Q2 数据脱敏 = C 分级脱敏（本地=PII / 云端=档位/掌握度/作业记录）
3. Q3 钢铁模式 = C 分层（小学强制 / 初中默认开 / 高中默认开可关）
4. Q4 AI 声明 = A UI 显式标签
5. Q5 教师审阅 = B 流程节点硬约束

**全闭环覆盖**：§2.2 4 步骤每个步骤都有 §7 合规约束对应

### §8 MVP 范围定稿（D-20）

13:42-13:46

5 拍板：
1. Q1 班级规模 = B 后端可配（UI 按 50 优化）
2. Q2 MVP 周期 = **不必设周期**（反向默认）
3. Q3 MVP 关键路径 = A 跑通 §2.2 4 步闭环
4. Q4 MVP 试用启动 = B 星云本人先跑 1 班验证
5. Q5 MVP 设备 = C 4 件清单（高速扫描仪 + 打印机 + 教师电脑 + 答题卡印刷品）

### §9 技术选型定稿（D-21）

14:31-14:35

**关键约束**：「**不能阻碍前路**」（大区域 / 全学段 / 全学科）
**星云授权**：§9 技术性问题星云没辨识能力，明确授权载曜做判断

5 拍板（3 项偏离默认）：
1. Q1 前端 = A React 18 + TS + Vite + AntD 5
2. **Q2 后端 = A FastAPI + Celery**（偏离默认 B「去 Celery」；Celery 依赖保留，MVP 不同步启用）
3. **Q3 数据库 = C 全程 PG**（偏离默认 B「SQLite → PG」；Docker Compose 简化）
4. **Q4 LLM = DeepSeek-V3 + Provider 抽象层**（偏离默认 A「DeepSeek 单选」）
5. Q5 部署 = Docker Compose 单机 + 无状态 FastAPI

**3 项偏离默认的取舍**：
- Q2 保留 Celery：大区域 / 全学科意味着分布式任务队列是扩展关键，MVP 阶段不用但保留依赖，零重构
- Q3 直接全程 PG：SQLite 在大区域 / 全学段 / 全学科扛不住（数据量大 + 多实例并发 + 跨表关联）
- Q4 加 Provider 抽象层：当前用 DeepSeek，但架构上可切 GPT-4o / Claude / 国产模型

**关键架构决策（避免未来重构）**：
- 无状态 FastAPI（横向扩展零成本）
- LLM Provider 抽象（多模型切换零重构）
- 数据模型预留学段 / 学科字段（v0.5 §1 已锁）
- Celery 依赖保留（分布式任务队列预留）
- OSS 对象存储抽象（MVP 本地文件夹实现）

**CHARTER §7.0 触发**：§9 拍板后，写新工程要在 subagent 工地，不在决策室

---

## 收工后状态

- **v0.5 定稿进度**：§0-§9 全定稿；剩 §10 排期 / §11 变更日志
- **CHARTER**：v0.2（全面重写）+ §7.0 会话分工（最重要）
- **下次唤醒第一句话**：「上次停在 v0.5 §9 技术选型定稿，下一步 §10 排期，开放问题见 docs/open-questions.md §10」

## 待澄清

- 暂无（§10 / §11 自然推进时涌现）
