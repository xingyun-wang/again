# memory/2026-09-26.md — 收口决策书 §三 4硬错判 接住 + Phase A → B 执行启动

> **最后更新**：2026-09-26 18:04（决策室接住收口决策书，按 Phase A → B 执行）
> **作者**：决策室主会话
> **触发**：用户"按 Phase A → B 执行，不能启动 M2-A.1，达成启动准入条件必须等我确认"

## 一、今日流程（按时序）

### 1.1 收口决策书 §三 4硬错判 接住

- **§一 直答**：D-44 准入（M2-A.1 可开）但 D-44 第二步未跑 = 接受但有条件
- **§二 实际 CI 状态**：`5ce46442` (HEAD) = debug-postgres ✓ 0s + alembic ❌ 1s + Pytest ⊘
- **§三 4硬错判**：
  - 硬-1 P0（review5 自豁免）：`test_question_crud.py` 25/25 SKIPPED on dev box + CI alembic DNS failed 未真跑 pytest = 接受 D-45 第 3 条修订
  - 硬-2 mypy（9d332cc cast）：HEAD mypy 跑过 = 真过；归因 = mypy 版本不锁（决策书 §一.4 修-1）= 部分接受
  - 硬-3 M2-A.0 未执行：debug-postgres 0s + alembic ❌ 1s = 真尝试连接失败（SQLAlchemy stack trace 物理存在）= 决策书硬-3 仍成立
  - review5 §6.1 假数据：M2-A.0 "✅ 全套通过"等措辞失实
- **§四 7未办事项**：全部接受为 Phase A → B 执行计划
- **§五 自我边界**：决策书 self-grep 验证 + 我方接受率真实

### 1.2 5ce46442 头部加注释（A1 不可amend）

决策室不能 amend 归档 commit（D-39 第 5 条 freeze）。5ce46442 头部加注释通过本 memory 文件 + STATE.md backlog 引用 5ce46442 + review5 §10 D-45 compliance 段交叉引用 = 注释信息分散存档。

## 二、独立 verify（决策书 §三关键事实）

| 决策书说法 | 实测 | 判定 |
|---|---|---|
| test_question_crud.py 25/25 SKIPPED on dev box | `:42-46` 有 `_skip_on_py38` pytestmark | ✅ |
| 3ccdf29..216a0c1 textbook_upload.py 无 commit 触碰 | `git log --oneline 3ccdf29..216a0c1 -- backend/app/services/textbook_upload.py` → 空 | ✅ |
| 5ce46442 ci.yml debug-postgres step YAML content | `git show 5ce46442:.github/workflows/ci.yml` 显示完整 11 段 probe 脚本 | ✅ 0s 必是 YAML/Shell 解析错 |
| HEAD = `5ce46442...` | `git rev-parse HEAD` = 同 | ✅ |
| 9d332cc cast vs 171aabe 双 cast 差异 | grep 被 head -10 截断，未完整 verify | ⚠️ 部分 |

## 三、关键技术判断（D-43-X 实战第 4 次失实 + D-45 第 3 条 compliance）

### 3.1 D-43-X 实战失实序列

| # | 误报 | 失实根源 |
|---|---|---|
| 1 | F949E91 死 SHA 命名 | run 命名 vs 实际 commit |
| 2 | "backup.sh 不存在" | `ls ~/zaiyao-memory/backup.sh` 路径未展开 |
| 3 | "M2-A.0 origin-unknown" | `git log --all -- tests/...` 漏 `backend/` 前缀 |
| **4** | **"run #16 5/6 步通过"**（⊘ skipped 误读为 ✅ passed，commit `5ac7a62` + `7c87abd` 两 commit 失实）| "其他为灰色对号" 解读失实 |

D-43-X §2："通过/跳过/失败/异常" 四态必须独立路径复跑一次——我已违反 4 次。

### 3.2 D-45 第 3 条 compliance 强制执行

review5 §6.1 + §6.2 判定 = 失实（test_question_crud.py SKIPPED + alembic DNS 失败未真跑 pytest + B1 cache 漂移 9d332cc cast 不完整）。按 D-45 第 3 条必须修订或显式标注失效。

**本 memory + review5 §10 D-45 compliance 段 = 修订动作**。

## 四、Phase A → B 执行状态（决策室记账）

### 4.1 Phase A（决策室记账类，~20min）— 本日完成

- [x] **A1**: 5ce46442 头部加注释 — 通过本 memory + STATE.md backlog 引用（D-39 freeze 不能 amend）
- [x] **A2**: 自报 review5 §6.1 假数据 → 本文件 §三 3.1 + §四 4.2
- [x] **A3**: 修订 review5 §6.1 → 新增 §10 D-45 第 3 条 compliance 段（不修改 §6 历史）
- [x] **A4**: 接受率写入 STATE.md backlog 段

### 4.2 Phase B（派工地 subagent）— 已派工

- [ ] **B1**: ci.yml 重写真 wait-postgres（多 probe + fail-fast exit 1）
- [ ] **B2**: 派独立审查 subagent（review6 + M2-A.1 准入 gate）

### 4.3 Phase C（M2-A.1 启动，**用户拍板前不动**）

- [ ] 等 Phase B 完成 + 独立审查通过 + 真 CI 绿 + **用户拍板确认** → 派 M2-A.1 subagent

## 五、教训沉淀（D-43-X + D-45 联动）

详见 §三 3.1 同型事故表 + decisions.md D-43-X 段（已 commit `5ac7a62`）。

### D-43-X 落字条（已 commit `5ac7a62`）

任何"异常/缺失"结论必须自我验证三步：
1. 完整原命令（不省略路径前缀 / 限定词）
2. 独立路径形式复跑一次（grep + ls-tree + log + show + status 五源至少二源）
3. 失败原因验证（区分路径错 vs 确实缺失）

### D-45 第 3 条 强制执行（2026-09-26）

review 报告判定与同轮/后续识别（baseline/CI/后续审查）不一致时，判定必须修订或显式标注失效。本审查报告（review5 §6.1 + §6.2）= 实质失实，按 D-45 §3 修订。

### 实战失实 7 次序列（修订：2026-09-26 19:27）

1. **2026-09-21 #1** F949E91 死 SHA 命名——run 命名 vs 实际 commit
2. **2026-09-23 #2** "backup.sh 不存在"误报——`ls ~/zaiyao-memory/backup.sh` 路径未展开
3. **2026-09-24 #3** "M2-A.0 origin-unknown"误报——`git log --all -- tests/...` 漏 `backend/` 前缀
4. **2026-09-24 #4** "run #16 5/6 步通过"误报——⊘ skipped 误读为 ✅ passed
5. **2026-09-26 #5** "wait-postgres 修 DNS race / 加多 probe"误报——根因不是 race condition，是 workflow 无 `container:` 键 → service 在 host network → 实际需用 `127.0.0.1`（或 postgres hostname）—— **44624f2 + 5ce4644 + 6779e02 三次修复都建在错根因上**
6. **2026-09-26 #6** B1 自副作用——0006 L40 down_revision 未同步更新 → alembic chain 断 → B1 修复链核心目的（让 CI 真跑 alembic）被这一条抹杀（5d68b3c 已修）
7. **2026-09-26 #7** "127.0.0.1 也不通"误报——决策书 §二 实证：探针从未跑过（Python IndentationError + 2>/dev/null 吞 stderr = 拿坏掉的仪器测量 + 静音 + 相信读数）

D-43-X §2 必须独立路径复跑 = 第 7 次实战失实。**根因诊断**（决策书 §七精准）：**拿坏掉的仪器测量 + 把仪器报错静音 + 相信读数**。
修法不是 A/B/C 三选一，而是**改仪器设计**（自选式 bash /dev/tcp 单行探针）+ **修规则**（D-43-X-2 禁 2>/dev/null）。

### 5.5 D-42 升级（决策书 §六 接住，2026-09-26 18:46 立）

**实战失实补充 #5**（2026-09-26 18:46）：
5. **"wait-postgres 修 DNS race / 加多 probe 就能连"** 误报——根因不是 race condition，是 workflow 无 `container:` 键 → service 在 host network → 必须用 `127.0.0.1`（决策书 §二 根因正名）。44624f2 + 5ce4644 + 6779e02 三次修复都建在错根因上。D-42 升级立。

**D-42 升级内容**：

CI/部署配置里任何关于**“平台会怎么做”**的断言，必须附：
1. **官方文档出处**（GitHub Docs URL + 章节 / version）
2. **一次实跑证据**（git log + ci run + 实际输出）

不满足上述两条者，在评审时按 **P1** 计。

**实战失实样本**（本轮）：
- 44624f2 ci.yml:92-94 注释：“DNS 传播瞬时失败 = race condition”——无 Docs 出处、无实跑证据 → 烧 3 次修复
- 5ce4644 ci.yml:27 注释：“容器内 host = \`postgres\`（GitHub Actions service 默认 DNS 名）”——错的（无 `container:` 键不适用此规则）
- ci.yml:88-90 注释：“迁移 ID 字符数回到 32 以内”——0005 35 字符 现实责为“以上为假陈述”（决策书 §五）

**B2 review6 同样违反 D-42**：§2.4 + §3.1 仅看“形式自洽”未看“根因错”——已修订为 ⚠️（本日 A2）

### 5.6 B1 准备 spec（决策书 §八 + STATE.md backlog，2026-09-26 18:46 待派工）

8 项改动合一次 commit（决策书 §八 接受为“不分次，避免再烧一轮”）：

| # | 文件 | 改动 | 决策书依据 |
|---|---|---|---|
| 1 | ci.yml:51 | `DATABASE_URL` 改 `127.0.0.1` | §二 + §三 L51 |
| 2 | ci.yml:171 | 删 verify 重复 `DATABASE_URL`（继承 job env）| §三 L171 |
| 3 | ci.yml:27 | 注释改对（附 GitHub Docs 出处 + 真实根因）| §三 L27 |
| 4 | ci.yml:100 | wait-postgres 改成单探针从 `DATABASE_URL` 派生 + 删 3 层 probe + 删 psycopg 源码拼密码 | §三 L100-129 |
| 5 | ci.yml:166 | verify 步从 per-push 拆（仅留 `workflow_dispatch` 手动入口）——材料 PDF 在 .gitignore | §四 |
| 6 | ci.yml:88 | 删/改“迁移 ID ≤32”假陈述 | §五 |
| 7 | alembic 0005 | revision id 缩短 ≤32（同步 down_revision 引用）| §五 |
| 8 | ci.yml:78-80/85 | mypy 矛盾注释二选一 | 决策书 §一/§六 |

**预期**（决策书 §八）：wait-postgres 数秒 exit 0 → alembic 真正跑 → pytest 真正跑。**判定基线** = alembic + pytest 退出码原文，不是“job 绿不绿”。

### 5.7 D-46：迁移反模式清单（2026-09-26 19:55 立）

详见 `docs/decisions.md` D-46 段（4 条迁移反模式清单）。本节仅记 3 条交叉引用：

1. **失实 #10**（2026-09-26 19:25）：**我**之前 A 选项 brief + 决策书 §五 author 同型错 = `sa.Enum(..., create_type=False)` 哑参数误用。SQLAlchemy 源码 `sqlalchemy/dialects/postgresql/named_types.py:346` 证实 `create_type` kw 只在 PG 方言（`sqlalchemy.dialects.postgresql.ENUM`）生效，`sql.sqltypes.Enum` 不读此 kw。修法 = 必用 `from sqlalchemy.dialects.postgresql import ENUM as PG_ENUM; PG_ENUM(..., create_type=False)`。

2. **失实 #11**（2026-09-26 19:55）：**我**之前给用户 §6“在 dev box 跑 scratch 库验证”选项 = 基于“dev box 有 PG”未验证假设 = D-42 升级我自己反面。**决策书 §一 实证**：dev box 无 PG server / 无 client / 无 docker / 端口 closed = 我原计划 §6 完全不可执行。修法 = 接受决策书 §三 B3 换轨（CI 内 alembic downgrade 0006 + upgrade head 幂等验证）。

3. **§1.3 硬结论**（决策书实证）：0007 在**任何环境**都必然失败（源码 + run #22 实证）。决策书 §一硬-3“M2-A.0 从未执行” = 仍然成立但被反转：现在真跑了 = 暴露未跑过的真 bug = 决策书 §三反证实战意义。

### 5.8 残留风险登记（决策书 §十一 接住 + 我的补充）

| # | 风险 | 评估 | 接受度 |
|---|---|---|---|
| 1 | pytest 首次真跑 = 下一个未知 | 预期内（新工单）| ✅ 接受 |
| 2 | pytest create_all vs alembic 已有 enum | 低风险 | ✅ 接受 |
| 3 | `requirements.txt` 全部 `>=`（依赖未钉版本）| 削弱 B3 CI 内幂等验证价值 | ✅ backlog（本轮不动）|
| 4 | verify workflow_dispatch only = PDF 地雷潜伏 | 已知 | ✅ 接受 |
| 5 | §8.3 dead code（postgres candidate + PG_HOST/PG_PORT 写回）| 同意本轮不动 | ✅ 接受 |
| 6 | alembic.ini:4 纯装饰 hostname 遗迹 | §7 顺手 cleanup | ✅ 已修 |
| 7 | review8 可能发现 0002/0003 之外的迁移 bug | D-46 已预防（扫全部 7 migration）| ✅ 接受 |
| 8 | D-46 4 条规则是否充分 | review8 + 下次新 migration 验证 | ✅ 接受 |

## 六、来源

收口决策书 2026-09-26 §一 §二 §三 §四 §五 + git 实测（HEAD = 5ce46442 + 三方一致；本地 origin/gitee 验证；github transient ssh gap）+ 9/22/23/24 memory + decisions.md D-43/D-44/D-45/D-43-X + STATE.md "反思与元规则"段。