# 2026-09-14

## 上午（已 reset，内容从 STATE.md / docs/decisions.md 推断）

v0.5 §0-§5 定稿（commit `db34d43`）。
11 个新决策（D-05 ~ D-11），详见 `docs/decisions.md`。

---

## 下午 — 工作流重构

16:21 soft reset，重新唤醒。
20:04 找到 `docs/产品定义-v0.5.md`（v0.5 是最新）。
20:15 星云解释 reset / 收工 / 唤醒的工作流。

### 新决策

- **D-12**：wake-up 固定 4 项 = 产品定义 / 最近 3 天日志 / 进度 / 宪法
- **D-13**：项目日志放项目本地（`tiered-homework-platform/memory/`），不再依赖 zaiyao-memory 全局目录
- **D-14**：灾难恢复 = 沿用 zaiyao-memory Gitee 仓 + 扩展 `backup.sh` 备份 4 项
- **D-15**：Gitee 远端清空重建——保留 12 文件（删 DREAMS.md），其余全清

### 产物

- 新建 `WAKEUP.md`（4 项唤醒清单 + 剪枝规则）
- 改 `backup.sh`：增加 4 项 + 最近 3 天日志备份
- 清 Gitee 远端：删 `.cache/` `.tmp/` `memory/` `reports/` `skills/` 3 个 `.lnk` + `DREAMS.md`
- force push 到 Gitee

---

## 待澄清

- 暂无（等 v0.5 §6-§11 推进时会自然涌现）

## 下一步

- 明天开始 v0.5 §6（数据采集）
- 见 `STATE.md` 的"下一步"

---

## 晚上 — 灾难恢复落地 + Gitee 远端清理

21:00+ 和星云对齐：4 项唤醒机制 + 选 A（沿用 zaiyao-memory 仓）+ 远端清空重建。

### 执行

- 创建 `WAKEUP.md`（4 项清单 + 剪枝规则 + 收工仪式）
- 重写 `zaiyao-memory/backup.sh`：从 rsync 镜像改清单模式 + 4 项 + 最近 3 天日志 + 修 mkdir bug + 加 workspace 自动检测
- 重写 `zaiyao-memory/README.md`：双用途（载曜记忆 + 项目 4 项灾难恢复）
- Gitee 远端清理：删 .cache / .tmp / memory / reports / skills / 3 .lnk / DREAMS.md，保留 12 文件
- Force push 成功：`bb6474b..2d5dc2c main -> main`
- 远端最终：17 文件（12 base + 5 项目快照）

### 新决策（D-12 ~ D-15，详见 docs/decisions.md）

- **D-12**：wake-up 固定 4 项
- **D-13**：项目日志放项目本地
- **D-14**：灾难恢复 = 沿用 zaiyao-memory 仓 + 扩展 backup.sh
- **D-15**：Gitee 远端清空重建

### 收工后状态

- 远端：17 文件，2 个新 commit（9f26a9a refactor + 2d5dc2c backup）
- 下次唤醒我会自动读 4 项：第一句话「上次停在 v0.5 §5，下一步 §6，开放问题见 docs/open-questions.md」
- 流程闭环：星云说"收工" → 载曜做 4 步 → 星云 reset

---

## 深夜 — CHARTER 重写

22:00+ 星云提了整改建议：旧 PROJECT-CHARTER（v0.1）是 v0.4 工程的产品定义副本，跟 v0.5 全面重新开始的状态脱节。

### 5 个拍板（D-16）

- **定位**：「硬约束沉淀 + 协作规则」（不是我建议 A 的「协作规则为主」）
- **§3 学科**：MVP 起点改「高中地理」（不锁年级）
- **§7/§8 整段删**：技术栈走 v0.5 §9、排期走 §10（CHARTER 不重复）
- **§5 合并**：题库 + AI 边界合 1 节（高度耦合，分开不自然）
- **不加传染索引**：v0.5 内部引用足够

### 产物

- `docs/PROJECT-CHARTER.md` 全面重写，6.2KB
  - §1 一句话定位（引 v0.5 §0）
  - §2 产品边界硬约束（✅❌ 清单）
  - §3 学科与用户边界（MVP 起点 + 平台最终覆盖 + §1.4 学科独立）
  - §4 档位（D<C<B<A）
  - §5 题库与 AI 边界（合并：来源 + 反马太 + AI 三类能力 + 共建愿景）
  - §6 合规边界（5 条不变）
  - §7 协作与灾难恢复（4 项 + 收工 4 步 + 决策记录 + 灾难恢复 + 改 CHARTER 触发）
  - §7 技术栈 / §8 时间表整段删
- `STATE.md`「最后更新」按 §7.5 同步标注
- `docs/decisions.md` append D-16

### Judgment calls（星云没明说，我替他判断了，flag 过了）

1. §3.1 MVP 章节：旧版锁「选择性必修 1 全 5 章」→ 新版改「由星云每学期决定精录章节」—— 星云默认按学期迭代走
2. §5.1 学科网 VIP 账号：旧版单独列 → 新版合并进「外部题库」—— 学科网是王星云已有
3. §7.4 灾难恢复：路径没改（沿用 9/14 落地版）

### 收工后状态

- CHARTER v0.2（首版是 v0.1 即 2026-08-23 旧文）落地
- 状态：v0.5 §0-§5 定稿 + CHARTER 重写 + 灾难恢复
- 下次唤醒第一句话：「上次停在 v0.5 §5 定稿 + CHARTER 重写，下一步 §6 数据采集，开放问题见 docs/open-questions.md §6」
