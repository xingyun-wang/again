# memory/2026-09-20.md — M1-B retro 全天流水

> **明早接手必读** — review2 子会话 status=done 但报告未投到主会话，第一件事查 `agent:again:dashboard:8687f80b-cbf6-4854-8cf8-241cb16298fd` 的 last messages。

## 战报（按时序）

1. **代码审查报告**（外部审查员投递）—— 13 处 P0/P1/P2，按 §7.6 三段式纪律合并 M1-B retro P0：
   - **P0-A 知识链路真实性**：content_summary / file_path / extraction_source 三处造假 → commit `30325f6`（merge A）
   - **P0-B owner_user_id** + 跨用户隔离：6 端点收下 `_user_id` 直接丢 → commit `43ff33a`（merge B）
   - **P0-C 流式 + 抗压 + 镜像卫生**：249MB 全量入内存 / 无 .dockerignore → commit `3499010`（merge C）
2. **G2 解封尝试**（子会话 timeout / 决策室接力成功）：alembic 0007 drop server_default + verify 段 B（≥1 个非 detected）+ service None 兜底 + D-37 负面测试 → commit `259aed3`（merge `19b1fc2`）
3. **D-31~D-39 决策落地**：审查准入 + 判级标准 + brief 中立性 + baseline 复核 + 豁免加锁 + 真实执行证据 + 二次审查 + 触发式 + 疲劳预算 + 归档 → commit `3a71cbb`
4. **第一轮独立审查**（commit `77095e6` 冻结）：静态模式，baseline B3/B4/B5 仍开 → 整体不通过 → D-39 归档 `docs/reviews/2026-09-20-77095e6.md`（commit `cdc76b9`）
5. **B4 解封 P0-1**（决策室）：`pyproject.toml` 删 `untyped-decorator`（mypy 不认这条 error code）→ commit `15f03a0`
6. **B3 解封 P0-2 + P1-3**（B3 工地 timeout / 决策室接力）：ci.yml PG service + 7 steps + conftest 双兼容 + verify 脱敏日志 + alembic-prep workaround（**B3 工地发现真 bug**：alembic_version VARCHAR(32) 与 33 字符迁移文件名冲突）→ commit `9d149c3`（merge `663b1f7` + tag `review/m1b-retro-b3-663b1f7`）
7. **第二轮独立审查**（commit `663b1f7` 冻结 / D-37 二次触发）：子会话 status=done（76K tokens，远低于 timeout 阈值）但 **报告未投到主会话** — 子会话 final message 可能为空或路由错位 → **明早接手第一件事**

## 当前 master 链（HEAD = `663b1f7`）

```
663b1f7 Merge M1-B retro B3: CI 闸门含 PG service + 真 pytest + 真 verify  ← 当前冻结
9d149c3 M1-B retro B3: ...                                   ← B3 决策室接 commit
15f03a0 D-36 解封 P0-1：mypy config 1 行修复
cdc76b9 D-39 归档：M1-B retro 第二轮独立审查（77095e6 冻结）
77095e6 Merge M1-B retro 工单 B: 归属与跨用户隔离 (owner_user_id)
43ff33a M1-B retro 工单 B ... D-29 B 项落地
19b1fc2 Merge M1-B retro G2
259aed3 G2 retro: extraction_source fail-open → fail-closed
3a71cbb M1-B retro: D-31~D-39 决策落地 + AGENTS.md 交叉引用
a072127 M1-B retro: 工作区清理（verifier 环境基线兼容修复）
3499010 M1-B retro P0-C: 流式 magic/cap/dockerignore/tests
```

## 仍未闭环的 baseline（carry-over 至今晚）

| # | 项 | 状态 | 解封路径 |
|---|---|---|---|
| B1 | `textbook_upload.py:407` mypy 错误 | 仍开（行号漂移） | 工地清理 / cast 局部修 |
| B2 | dev box pytest collect errors | 仍开 9 errors（py3.8 缺 psycopg） | B3 闭环后 CI 跑会变 |
| B3 | CI 闸门含 PG fixture | **配置闭环** / **执行未跑** | review2 报告 → 用户触发 CI → 第二轮实跑 |
| B4 | mypy config | **已闭环**（15f03a0） | ✓ |
| B5 | dev box 真 LLM + alembic | **配置闭环** / **执行未跑** | 同 B3 |

## B3 工地 timeout 模式（D-38 沉淀候选）

第 4 次同模式（A v3 / C v3 / G2 工地 / B3 工地）：
- 工地 brief 太大（≥3 改动 + 多 D-XX 硬规则）
- 子会话进入"无可见输出"的 debug 死循环
- token 耗尽前不交付

**建议沉淀**（待明早决定）：
- D-38 加 "失败模式" 段落
- 下个 milestone 拆 brief ≤15min 单步，spawn 多个短任务而非一个长任务

## 明早接手第一件事

**查 review2 报告**：
- sessionKey `agent:again:dashboard:8687f80b-cbf6-4854-8cf8-241cb16298fd`
- 命令：`sessions_history sessionKey=<上述> limit=20`
- 子会话 status=done 但报告未投到主会话；需查 last messages 看实际交付了什么

## M2 解封路径两条（要星云拍）

| 选项 | 星云动作 | 结果 |
|---|---|---|
| A 触发 CI | github repo 触发 `review/m1b-retro-b3-663b1f7` tag 的 workflow | 5 命令真跑 + 真 LLM verify → review2 报告升级「实跑」 → M2 解封 |
| B 不触发 | 等 | review2 报告「静态 + 部分实跑」 → M2 解封延后到 B3 真跑过 CI |

## 其他注意

- zaiyao-memory 4 项快照今天未跑（dev box 上没找到 backup.sh） — 明早接手时检查
- §10.6 中 D-38 疲劳预算表「失败模式」段落待沉淀
